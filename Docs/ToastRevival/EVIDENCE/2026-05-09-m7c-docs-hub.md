# M7.C — Public Docs Hub (`/docs`)

Date: 2026-05-09 (Saturday, UTC ~13:26)
Author: DocPro team — Carl, Anthony, Diana, Abish
Production URL: https://toastnotification.com/docs

## Scope

Diana's spec at `Docs/ToastRevival/DESIGN-SPEC.md` lines 418–446 (Docs section) and lines 720–725 (M7.C
acceptance criteria). Six new public unauth marketing routes plus shared sidebar layout.

| Route | Purpose |
|---|---|
| `/docs` | Hub overview — quick-link cards, platform fit, endpoint conventions, security posture. |
| `/docs/getting-started` | Sign up → register tenant → install agent → send first notification. |
| `/docs/deploy/store` | Microsoft Store install path (env-vars / bootstrap.json tenant binding). |
| `/docs/deploy/intune` | Intune LOB upload + tenant-id delivery options + WDAC/AppLocker note. |
| `/docs/deploy/rmm` | NinjaOne / Datto / ConnectWise Automate / Atera / generic msiexec patterns. |
| `/docs/api` | REST API reference — auth, devices, notifications, webhooks, rate limits. |

## Files added (committed)

- `src/ToastRevival.Dashboard/src/components/marketing/DocsLayout.tsx` — 240px sticky sidebar (top: 80px) + outlet, mobile collapsible nav.
- `src/ToastRevival.Dashboard/src/components/marketing/CodeBlock.tsx` — copy-to-clipboard `<pre><code>` block + `<Callout>` (note / warning).
- `src/ToastRevival.Dashboard/src/pages/marketing/docs/DocsIndex.tsx`
- `src/ToastRevival.Dashboard/src/pages/marketing/docs/DocsGettingStarted.tsx`
- `src/ToastRevival.Dashboard/src/pages/marketing/docs/DocsStore.tsx`
- `src/ToastRevival.Dashboard/src/pages/marketing/docs/DocsIntune.tsx`
- `src/ToastRevival.Dashboard/src/pages/marketing/docs/DocsRmm.tsx`
- `src/ToastRevival.Dashboard/src/pages/marketing/docs/DocsApi.tsx`

## Files modified (committed)

- `src/ToastRevival.Dashboard/src/App.tsx` — six new lazy chunks + nested route group under `MarketingLayout`.
- `src/ToastRevival.Dashboard/src/components/marketing/MarketingHeader.tsx` — `Docs` nav link added (desktop + mobile menu).
- `src/ToastRevival.Dashboard/src/components/marketing/MarketingFooter.tsx` — `Resources` column (Docs / Getting started / API reference). Slim grid widened 3→4 cols.
- `src/ToastRevival.Dashboard/src/components/marketing/marketing.css` — `.m-docs-*` namespace appended (~470 lines), mobile breakpoint with 44px touch targets, slim grid 4-col.

## Files NOT touched (Codex's in-flight WIP per `Docs/ToastRevival/CODEX-HANDOFF.md`)

39 modified tracked files + 5 new untracked covering Pricing v2 (collapse `SubscriptionTier`,
`LicenseService` rewrite, `BillingController` shape change, Stripe per-unit metered model, 14-day
`TrialPeriodDays`), PlatformAdmin role (`AppUser.IsPlatformAdmin`, `SystemController`,
`PlatformAdminBillingV2` migration, `BillingPlanRules`, `IStripeBillingSyncService` /
`StripeBillingSyncService`), and the admin dashboard UI redesign across all 13 dashboard pages,
`Sidebar.tsx`, `Layout.tsx`, `AuthContext.tsx`, `index.css` (+405 lines of light-mode tokens).

Codex re-edited `pages/marketing/Home.tsx` (-3 / +3, em-dash → hyphen, copy aligned to new Stripe
trial flow) and `pages/marketing/Pricing.tsx` (-15 / +15, similar alignment). Both preserved
unstaged.

## Cross-AI handoff pattern (rerun)

Tonight is the second time we've used the M7.B selective-commit handoff. Steps:

1. `git add` only the M7.C named paths.
2. `git stash push --keep-index -u -m "wip-codex-preserve-m7c"` — sets aside Codex's tracked + untracked WIP.
3. **Isolated build**: `cd src/ToastRevival.Dashboard && npm run build` — 729 modules clean ✓.
4. `git stash pop` — restores Codex WIP.
5. **WIP-included build**: `npm run build` — 729 modules clean ✓. Marketing CSS bundle includes M7.C docs styles AND Codex's index.css additions.
6. Deploy WIP-included build (preserves prod parity with Codex's deployed chrome).
7. `git commit` only the staged M7.C files.
8. `git push`.

Both builds were run; both clean.

## Build artifacts (WIP-included, deployed)

```
dist/index.html                            0.78 kB
dist/static/index-CPBLaXIl.css            10.27 kB │ gzip:   2.79 kB
dist/static/MarketingLayout-BSpRHU8M.css  28.88 kB │ gzip:   5.19 kB   (+8 kB from M7.B for docs CSS)
dist/static/CodeBlock-DytdGNV3.js          1.67 kB │ gzip:   0.75 kB
dist/static/DocsLayout-B-ZERtfF.js         1.83 kB │ gzip:   0.84 kB
dist/static/DocsIndex-DUIJW4hk.js          4.75 kB │ gzip:   2.02 kB
dist/static/DocsStore-CVoVqrWI.js          4.80 kB │ gzip:   1.85 kB
dist/static/DocsGettingStarted-Qgmh9rls.js 5.85 kB │ gzip:   2.13 kB
dist/static/DocsIntune-UOejq7VW.js         6.66 kB │ gzip:   2.47 kB
dist/static/DocsRmm-C4E-Ni-a.js            6.83 kB │ gzip:   2.41 kB
dist/static/DocsApi-DIZi9PPP.js           10.96 kB │ gzip:   3.33 kB
dist/static/Pricing-D1LJk3zr.js            9.55 kB │ gzip:   3.52 kB   (Codex copy edits)
dist/static/Home-CexJazwF.js              11.01 kB │ gzip:   3.49 kB   (Codex copy edits)
dist/static/index-ChfFBhOb.js            723.82 kB │ gzip: 212.61 kB   (+2 kB from Codex page TSX)
✓ built in 4.55s
```

## Deploy procedure

```bash
tar -cz -C src/ToastRevival.Dashboard/dist -f /tmp/toast-dashboard-m7c.tgz .
scp -i Docs/Assets/Toast_Web_LightsailDefaultKey-us-east-1.pem \
  /tmp/toast-dashboard-m7c.tgz ubuntu@54.82.103.160:/tmp/
ssh -i ... ubuntu@54.82.103.160 \
  'set -e; \
   sudo cp -a /opt/toast/dashboard /opt/toast/dashboard.bak.m7c; \
   sudo find /opt/toast/dashboard -mindepth 1 -delete; \
   sudo tar -xzf /tmp/toast-dashboard-m7c.tgz -C /opt/toast/dashboard; \
   sudo chown -R toast:toast /opt/toast/dashboard'
```

Backup retained at `/opt/toast/dashboard.bak.m7c` for rollback.

## Post-deploy verification

```
HTTP / bytes
  /static/index-C-HXF1Mu.js    200 / 723817   (main bundle)
  /static/DocsIndex-CrIQZnjU.js 200 / 4753    (docs hub chunk)
  /static/DocsApi-7e508JIo.js   200 / 10957   (api ref chunk)
  /docs                         200 / 782     (SPA fallback)
  /docs/api                     200 / 782     (SPA fallback)
  /login                        200 / 782     (FIX-PROD-001 standing check)
```

Playwright screenshots (full-page, locally captured pre-cleanup):

- `m7c-docs-index.png` — `/docs` at 1440×900 desktop. Sidebar `Overview` active, 5-card hub grid, footer 4-col with Resources column.
- `m7c-docs-api.png` — `/docs/api` at 1440×900 desktop. Endpoint tables with method pills, code blocks with Copy button, callouts.
- `m7c-docs-rmm-mobile.png` — `/docs/deploy/rmm` at 480×900. Hamburger top, "Documentation menu" toggle, content stacks, code blocks horizontal-scroll.
- `m7c-login-regression.png` — `/login` at 1440×900. SPA renders cleanly. **No marketing-page-clobber. FIX-PROD-001 hold confirmed.**

## Code Sweep (Abish)

**Verdict:** SHIP WITH NOTES.

INFO findings:

- **INFO-M7C-001** — `CodeBlock` clipboard API has no fallback for sandboxed/insecure contexts. Fails silently (button stays default state). Acceptable v1; M9 polish candidate.
- **INFO-M7C-002** — `m-footer-grid--slim` widened 3→4 cols to host new Resources column. Codex did NOT modify `MarketingFooter.tsx` (verified via `git diff --cached`); no conflict.
- **INFO-M7C-003** — Docs reference `Devices → Install agent` admin tab. Surface may not yet exist on prod; copy is forward-leaning. Confirm or add to M8 follow-up.
- **INFO-M7C-005** — (Diana, post-sign-off) Body color `--text-secondary` (#4B5563) reads slightly soft on `--bg-primary` (#F3F5F8). WCAG AA compliant but Keith may flag. M7.D candidate.

Pre-commit fix:

- **FIX-M7C-001** — Mobile docs nav links bumped from ~36px to 44px min-height (Diana standing rule). Patched mid-sweep, rebuilt, re-staged.

## Diana sign-off

**APPROVED.** Type pairing locked, sidebar active state correct, mobile touch targets at 44px,
endpoint table with pills reads, callout colors match `--status-success` / `--status-warning`,
code-block JetBrains Mono with Copy button anchored top-right.
