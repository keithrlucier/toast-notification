# v0.4.38 Deploy Verification — 2026-06-03

Review-remediation release (2026-06-02 cold review). Closed-loop remediation of
`REVIEW_LEDGER.md`: 6 actionable findings resolved + verified, XT-M1 stays Keith-owned/OPEN.

## What shipped (agent-side, reaches fleet via signed MSI)

- **Agent-M1** — `SelfUpdateService.cs`: `EnsureProtectedVerifiedDir` now deletes ANY
  pre-existing `verified\` entry (plain dir wholesale, reparse unlink-only) and
  `LockDownToSystemAndAdmins` calls `SetOwner(SYSTEM)` before the DACL. Closes a local
  non-admin → SYSTEM path via the non-Authenticode-gated `apply-msi.cmd`; resolves the
  Agent-L1 anchor-challenge.
- **Wix-M1** — `ToastRevival.Agent.Setup.wxs`: new `UninstallApplyTask` removes the
  orphaned `\Toast2IT\ToastNotificationApplyUpdate` SYSTEM task on `msiexec /x`.

(RMM script fixes RMM-H1/H2/M1 + the RMM-M2 anchor shipped with commit `861ec07`; the
updated `uninstall-toast-agent.ps1` is also re-served from the fleet download path — see Deploy.)

## Signing

| Artifact | Status | Cert | Valid To |
|---|---|---|---|
| `ToastNotification.Agent.exe` | Valid | CN="Toast2IT, LLC" (Sectigo OV) | 2027-04-15 |
| `ToastNotification.Agent-0.4.38.0.msi` | Valid | CN="Toast2IT, LLC" (Sectigo OV) | 2027-04-15 |

Both signed via Thales SafeNet token + `scripts/sign-msix.ps1` (signtool `/fd SHA256 /tr digicert`),
DigiCert RFC-3161 timestamp present. Thumbprint `19B07B46712C2D87FF6AA99842F7EF6B036FEDA7`.
Build order: publish self-contained 0.4.38 → sign exe → `wix build` (packages signed exe) → sign MSI.

## Deploy (TOASTWEB1 — ubuntu@54.82.103.160)

- Current `ToastNotification.msi` backed up → `ToastNotification.msi.bak-0437`.
- Signed MSI placed at `/opt/toast/downloads/ToastNotification.msi` AND
  `/opt/toast/downloads/ToastNotification.Agent-0.4.38.0.msi` (toast:toast, 80,658,432 bytes).
  Deployed SHA256 == local signed SHA256 `5ced4ee2…4475ceb` (canonical + versioned identical).
- Updated `uninstall-toast-agent.ps1` re-served at `/opt/toast/downloads/uninstall-toast-agent.ps1`
  (toast:toast, 22,552 bytes, SHA256 `649629de…b34d557`) per the DEPLOY-PATHS §1 standing rule.
- `Agent__LatestVersion=0.4.38` set in `/opt/toast/.env` (chown toast:toast, chmod 600) AFTER the
  MSI was confirmed served (inert-window order). `toast-api.service` restarted → active.

## Verification

- `curl https://toastnotification.com/api/agent/version` → `{"version":"0.4.38","msiDownloadUrl":".../ToastNotification.msi"}` ✓
- `curl -sI .../downloads/ToastNotification.msi` → HTTP/2 200, content-length 80,658,432 ✓
- `curl -sI .../downloads/uninstall-toast-agent.ps1` → HTTP/2 200 ✓
- Marketing home → 200 · SPA `/login` → 200 · `/api/health` → 200 ✓

## Not done this session (known, non-blocking)

- **Velopack feed** (`/opt/toast/releases/agent/win-x64/`) is stale at 0.4.32 — has been since
  before 0.4.37; the MSI/RMM channel is how the fleet actually updates. Setup.exe-channel installs
  (minority) won't see 0.4.38 until the Velopack release is rebuilt. Tracked as a follow-up.
- **XT-M1** — machine-SID token binding, Keith-owned build-project XT-3. Stays OPEN.

## Git

- Commits: `861ec07` (remediation fixes + ledger), `11ae400` (agent 0.4.37→0.4.38 version bump),
  appsettings LatestVersion bump (this commit). Tag `v0.4.38` pushed to origin.
- Public mirror: `toast-notification` `45d3b23` + tag `v0.4.38` (source-available, RMM scripts excluded by design).
