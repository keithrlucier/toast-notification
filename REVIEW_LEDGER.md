# REVIEW LEDGER — Cold Code Review (2026-06-03) + Comprehensive Engineering Review (2026-06-05)

```
Read REVIEW_LEDGER.md / latest review_history?   Yes (prior ledger REVIEW_LEDGER_2026-06-03_1.md + REVIEW_LEDGER_2026-06-02_1.md + REVIEW_LEDGER_2026-06-02_2.md read; all prior items verified)
Closed-pass anchors honored?                       Yes (REVIEW Agent-L1 updated in code; RMM-M2 REJECTED-by-design anchor in Reset-ToastLockScreen.ps1; XT-M1 carve-out anchor in DevicesController.cs; BLK-1 NormalizeForMatch anchor in BlocklistService.cs)
Files scanned:                                     ~95 (full workspace sweep: 4 RMM PS1 scripts; WiX installer; Agent C# src (SelfUpdateService, AgentClient, UpdateService, DesktopOverlayService, TenantLogoStore); API Controllers (Devices, Blocklist, Notifications, Moderation, Templates, Audit, Health); API Services (BlocklistService, NotificationQueueService, NotificationPayloadBuilder, BillingPlanRules, HttpContextTenantProvider); AppDbContext; Dashboard TSX (Devices, EnrollmentTokens, Compose, Auth, ProtectedRoute))
Files with anchors found and respected:            6 (SelfUpdateService.cs, DevicesController.cs, BlocklistService.cs, Reset-ToastLockScreen.ps1, AuthContext.tsx, install-toast-agent.template.ps1)
```

> **Comprehensive engineering review added 2026-06-05 (ref: `Docs/ToastRevival/REVIEW-2026-06-05.md`).**
> 148 findings · 12 perspectives · 2 Critical · 19 High · 61 Medium · 64 Low · 2 Info.
> All items tagged with IDs below start OPEN. Verify cited file:line before scoping each fix —
> cold reviews run against a snapshot; stale line numbers are common. XT-M1 remains OPEN per Keith.

> **Cold pass — 2026-06-03 (Carl + 4 parallel Explore agents).** Scope: full workspace sweep
> (not bounded to recent commits). Prior ledger archived to
> `Docs/review_history/REVIEW_LEDGER_2026-06-03_1.md`. Prior closed rows (FIXED-VERIFIED /
> REJECTED-VERIFIED) not re-derived. XT-M1 carried forward (OPEN by design, Keith's product
> decision, unchanged since 2026-06-02).
>
> **False-positive triage note:** 6 "Critical IDOR" candidates surfaced by one agent across
> `BlocklistController.List()`, `NotificationsController.History()`, `ModerationController.GetPending()`,
> `ResolveTargetDeviceIds()` (All + Device paths), `NotificationDeliveries` fetch, and
> `BlocklistService.CheckAsync()`. Personally verified against `AppDbContext` — **all six are
> false positives**. Every cited entity has an EF global query filter keyed to
> `_tenantProvider.TenantId` (lines 53, 79, 88, 101, 117, 152, 165, 182 in `AppDbContext.cs`);
> `HttpContextTenantProvider` reads the JWT `tenantId` claim in-request, returning null outside a
> request context (which makes filters produce `WHERE tenant_id IS NULL` — zero rows). Background
> processing in `NotificationQueueService` correctly uses `IgnoreQueryFilters()` throughout. No
> IDOR exists.

## Gauge rules (v5.4.25 parser — DO NOT BREAK)

Every pipe-table row whose first cell is an ID (letter + digit) counts as OPEN **unless**
the row also contains one of these uppercase terminal tokens:
`FIXED-VERIFIED` · `REMEDIATED` · `REJECTED-VERIFIED` · `VERIFIED-CLEAN`

`DEFERRED` / `BLOCKED` / `OPEN` all **count as OPEN**. An in-code anchor is NOT a terminal
state. Only code changed + verified earns a terminal token. Prose sections (ANCHOR-CHALLENGE,
"Reviewed and clean") use no pipe rows and do not affect the count.

---

## Critical

| ID | Severity | Status | File:Line | What's wrong | Why it matters | Confidence |
|---|---|---|---|---|---|---|
| MT-C1 | Critical | OPEN | `AppDbContext.cs:43,53,62,79,88,101,117,126,152,165,182`; `ITenantProvider.cs:7` | `ITenantProvider.TenantId` is declared `Guid?`. EF Core global query filters expressed as `e.TenantId == _tenantProvider.TenantId` translate `null == null` to `WHERE TenantId IS NULL` in SQL — returning **zero rows**, not unfiltered data. The comment in `ITenantProvider.cs` says "null as no filter" which is factually wrong. Background services or webhook handlers using the same DbContext without a tenant identity silently receive empty result sets, masking bugs. Any code relying on "null means no filter" fails silently. | Cross-tenant data exposure if filter is bypassed or null-TenantId is intentionally used for cross-tenant reads. Silent bug masking in background service paths. Incorrect comment actively misleads future developers. Integration tests don't exercise the null-TenantId path. A future `IgnoreQueryFilters` call anywhere on these paths would expose full cross-tenant data with no fallback guard. | High |
| DEVOPS-C1 | Critical | OPEN | `Docs/ToastRevival/.env`; `.env.pats` | Plaintext credentials on disk: `.env` holds AWS Lightsail admin password, a GitHub PAT (`github_pat_11AJ3OCYA0LFuxLN…`), production server IP (`52.21.249.120`), and SSH port. `.env.pats` holds `TOAST_PRIVATE_PAT` (repo-scope on private source repository) and `TOAST_PUBLIC_PAT`. Both files are gitignore-excluded but exist unencrypted alongside the repo — any zip, tarball, or accidental mirror sync exposes them. No pre-commit hook (git-secrets / truffleHog) exists to block future credential staging. | Private PAT has full `repo` scope — enables read of all private source and write to any branch. Production SSH credentials exposed. One accidental `git add` or mirror sync exposes full prod access. No automated guard exists to prevent recurrence. | High |

## High

| ID | Severity | Status | File:Line | What's wrong | Why it matters | Confidence |
|---|---|---|---|---|---|---|
| DOS-H1 | High | OPEN | `AuthController.cs:202` | `POST /api/auth/forgot-password` has no `[EnableRateLimiting]` attribute and no Turnstile challenge. An attacker can POST any valid email in a tight loop, causing continuous `GeneratePasswordResetTokenAsync` + Mailjet `SendAsync` calls with zero throttle from a single IP. | Drains Mailjet quota and constitutes targeted email harassment against any known user address. No cost to attacker, direct financial + reputational cost to operator. | High |
| DOS-H2 | High | OPEN | `AuthController.cs:135` | `POST /api/auth/register/verify-sms` has no `[EnableRateLimiting]` attribute. `RegisterFailedSmsAttemptAsync` is NOT called on a wrong-code path. 6-digit OTP = 10^6 space; attacker faces no per-IP throttle and no lockout counter — effectively unlimited guesses within the 10-minute OTP window. | Registration OTP fully brute-forceable. Attacker can register or verify accounts on victim email addresses with zero friction. | High |
| DC-H1 | High | OPEN | `Models/TenantApiKey.cs`; `Migrations/20260509035139_M5ApiKeyManagement.cs` | Full `DbSet<TenantApiKey>` registered, migration creates the `TenantApiKeys` table with `KeyHash`, `KeyPrefix`, and FK to `Tenants`. Zero controllers query or mutate `TenantApiKeys`. The table exists in production, holds no data, and carries ongoing index maintenance overhead on every write cycle. | Dead schema in production. Migration history misleads future developers into assuming a live controller exists. Index overhead on every `Tenants` write. Either build the API key management feature or drop the table with a new migration. | High |
| DC-H2 | High | OPEN | `Models/Tenant.cs:24`; `BillingController.cs:356` | `Tenant.LicenseCount` is always written as `0` in BillingController webhook paths but never read in any business logic. `LicenseService.IsWithinCap` reads `ConsumedCount` and `BillingPlanRules` — never `LicenseCount`. The codebase moved to per-device billing, making this seat-count column irrelevant dead weight. | Write-only column actively misleads any developer who reads the model. DB column + ORM overhead for zero business value. Should be dropped via migration. | High |
| MT-H1 | High | OPEN | `DevicesController.cs:170-193` | `List()` and `Get()` rely entirely on the EF Core global filter for tenant isolation with no explicit `d.TenantId == tenantId` predicate. All mutation endpoints (Decommission, RequestUninstall, etc.) explicitly check `TenantId` from JWT claims — the read-only paths have no such backstop. | A filter misconfiguration, null TenantId in request context, or future `IgnoreQueryFilters` call silently returns cross-tenant device data. Single implicit layer is the only guard on these high-traffic read paths. | High |
| MT-H2 | High | OPEN | `NotificationsController.cs:243-267` | `GET /api/notifications` (History) queries `_db.Notifications` with only `.OrderByDescending(n => n.CreatedAt)` — no explicit `n.TenantId == tenantId` predicate. Tenant isolation entirely delegated to the global filter. | Same single-layer risk as MT-H1 on the notification history path. A broken filter exposes cross-tenant notification history. | High |
| MT-H3 | High | OPEN | `AnalyticsController.cs:34-131` | `Summary()`, `Volume()`, and `Breakdown()` query without explicit `TenantId` predicates. The comment in `Breakdown` states "Both sides carry global tenant query filters so no explicit TenantId filter needed" — this is architecturally incorrect reasoning and should be removed. | Cross-tenant analytics blending with no detection mechanism. Comment actively misleads future developers. Three endpoints, all single-layer guarded. | High |
| MT-H4 | High | OPEN | `DeviceGroupsController.cs:127-129, 160-165` | `AddMember` checks `_db.Devices.AnyAsync(d => d.Id == req.DeviceId && d.Status != DeviceStatus.Decommissioned)` relying solely on the global filter. `SetMembers` validates device IDs against `_db.Devices` without explicit `d.TenantId == GetTenantId()`. A tenant admin could potentially add a device from another tenant to their group if the filter is bypassed. | Cross-tenant device group membership manipulation. Both the existence check and the bulk-set path are unguarded. | High |
| MT-H5 | High | OPEN | `NotificationsController.cs:625-647` | `ResolveTargetDeviceIds` — both `TargetType.All` and `TargetType.Device` branches base on `_db.Devices.Where(d => d.Status == DeviceStatus.Active)` with no explicit `.Where(d => d.TenantId == tenantId)`. `TargetType.Group` is correctly guarded. If the global filter is null or bypassed, `TargetType.All` resolves to ALL active devices across ALL tenants — dispatching a notification fleet-wide. | Worst-case vector: cross-tenant notification blast to entire enrolled fleet if filter is null or bypassed. `TargetType.Group` path proves the team knows to add explicit predicates — the inconsistency is the risk. | High |
| PERF-H1 | High | OPEN | `AnalyticsController.cs:46-51`; `AnalyticsController.cs:104` | `Summary()` — `_db.NotificationDeliveries.Where(d => d.CreatedAt >= since).Select(d => d.Status).ToListAsync()` materializes every delivery status enum for the period into .NET process memory, then counts with in-process LINQ. `Breakdown()` does the same. For a tenant with 10,000 devices and frequent sends over 90 days, this loads millions of rows. | Unbounded memory growth per analytics request. Large tenants will cause severe latency spikes and OOM risk. This should be a server-side `GROUP BY` projection — one SQL row per status, not millions of hydrated enums. | High |
| PERF-H2 | High | OPEN | `AuditController.cs:86-89` | `Export()` — `ToListAsync()` loads all matching audit rows for the requested time range into `List<AuditLog>` before calling `GenerateAuditLogPdf` or `BuildAuditCsv`. Each row carries the full `DetailsJson` blob. No row cap. A single 90-day export for a high-activity tenant can spike process memory by hundreds of MB. | Uncapped memory spike per export. Can exhaust server process memory on a single request. Pairs with PERF-H3 (synchronous PDF rendering on the same thread) for a compounding worst case. | High |
| PERF-H3 | High | OPEN | `PdfExportService.cs` | Both `GenerateAuditLogPdf()` and `GenerateDeliveryReportPdf()` are fully synchronous (`byte[]` return, no async) and run entirely on the calling thread pool thread. QuestPDF's `GeneratePdf()` is CPU-intensive. Under concurrent export requests, thread pool threads are blocked and unavailable to serve other requests. | Thread pool exhaustion under concurrent export load. No async path exists. Combines with PERF-H2's unbounded data materialization for a compounding risk. Should run in `Task.Run` at minimum; streaming async is the correct fix. | High |
| REL-H1 | High | OPEN | `Program.cs` (middleware pipeline) | No `UseExceptionHandler` or equivalent global middleware. Unhandled exceptions (DbUpdateException from race conditions, ArgumentNullException in service constructors, Stripe SDK exceptions in CreateCheckout/CreatePortal) surface as raw Kestrel 500s with no central logging hook, no structured error response, and no single monitoring point. | Unobserved exceptions produce no actionable telemetry. Four distinct error shapes already exist across controllers (raw string, `{message}`, `{error,message}`, `{errors}` array) — unhandled exceptions add a fifth. Blocks production monitoring. | High |
| REL-H2 | High | OPEN | `BillingController.cs:291` | `_ = Task.Run(() => HandleStripeEventAsync(stripeEvent, services))` — fire-and-forget capturing `HttpContext.RequestServices` (request-scoped `IServiceProvider`). If the request scope is disposed before the Task completes, `ObjectDisposedException` is thrown and silently lost. No backpressure: a burst of webhook events queues unlimited thread-pool tasks with no retry, no dead-letter queue, and no idempotency key check (`evt.Id` never stored). | Stripe payment events silently lost on request-scope disposal or app shutdown. Financial data loss. Duplicate webhook delivery causes double-processing of checkout completion and invoice payment. | High |
| REL-H3 | High | OPEN | `MailjetEmailService.cs:44`; `ClickSendSmsService.cs:41` | Both services call `PostAsync` then `EnsureSuccessStatusCode` with no retry policy, no circuit breaker, and no per-call timeout beyond the default 100-second HttpClient default. A transient 429/5xx or hung connection immediately throws `HttpRequestException` with no retry. | MFA login blocked for the full 100-second duration of a ClickSend outage. Email delivery fails permanently on transient Mailjet errors. Thread pool exhaustion when both providers simultaneously degrade — each request holds a thread for up to 100 seconds. | High |
| DEVOPS-H1 | High | OPEN | `docker-compose.yml:38` | `Jwt__MfaElevationExpiresInMinutes: 480` is hardcoded. `appsettings.json` correctly sets this to 15 minutes. The docker-compose override sets it to 480 — identical to the standard session token lifetime. In any Docker-deployed environment, MFA elevation tokens live for 8 hours, completely defeating their purpose. | All `RequireFreshMfa()` guards across `SystemController` (write operations: create tenant, approve trial, update billing) and `BillingController` (Stripe config write) accept tokens up to 8 hours old. The step-up security model is defeated in production. | High |
| DEVOPS-H2 | High | OPEN | `src/ToastRevival.Api/Dockerfile` | `dotnet publish -c Release` with no `DebugType` suppression and no `.dockerignore` copies PDB files into the runtime image. A PDB maps IL offsets to source file paths and line numbers, enabling anyone who obtains the image to reverse-engineer internal class and method names and map exception stack traces to exact source lines. | Attacker with container image access (registry breach, container escape) gains a source-level roadmap for targeted exploitation. Significantly lowers the bar for any post-breach attack. Fix: add `<DebugType>none</DebugType>` to the Release `PropertyGroup` in `ToastRevival.Api.csproj`. | High |
| DEVOPS-H3 | High | OPEN | Repository root — no `.dockerignore` present | Without `.dockerignore`, the full build context is sent to the Docker daemon on every build, including `Docs/` (containing `.env` with credentials), `scripts/`, `.env.pats`, and all local build artifacts. Any future broadened `COPY` step would materialize credentials in build layers that may be pushed to a registry. | Build context includes plaintext credentials. A single `COPY .` or `COPY Docs/` in the Dockerfile leaks them into image layers. Risk compounds if the image is pushed to a shared or public registry. | High |
| DEVOPS-H4 | High | OPEN | `c:\SOURCE\toast\.github\workflows\` (directory is empty) | Despite Dependabot configured, zero workflow files exist. All build, test, sign, and deploy steps are entirely manual PowerShell scripts. No automated gate on PRs, no automated Docker image build, no deployment pipeline. A breaking change can merge and be deployed with zero automated verification. | No CI means breaking changes ship undetected. The recent MSI binary version mismatch (0.4.38 binary shipped in 0.4.40 package) is exactly the class of error a `dotnet build` + `dotnet test` gate would have caught. | High |
| WSEC-H1 | High | OPEN | `UpdateService.cs:264`; `SelfUpdateService.cs:706` | Both `IsSignedByToast2IT` implementations call `WinVerifyTrust` with `fdwRevocationChecks = 0` (`WTD_REVOKE_NONE`). A Toast2IT code-signing certificate that has been compromised and subsequently revoked by Sectigo passes this verification gate without any error. The MSI update path (`SelfUpdateService`) results in SYSTEM-level `msiexec` execution. | A revoked signing certificate enables compromised-certificate update delivery to every enrolled device, with SYSTEM-level execution on the MSI path. Revocation checking is the last line of defense after a certificate compromise event. Fix: set `fdwRevocationChecks = WTD_REVOKE_WHOLECHAIN (0x00000002)` in both structs. | High |

## Medium

| ID | Severity | Status | File:Line | What's wrong | Why it matters | Confidence |
|---|---|---|---|---|---|---|
| RMM-M3 | Medium | FIXED-VERIFIED | `infrastructure/rmm/install-toast-agent.template.ps1:81-90` | The Authenticode signature check is explicitly labelled "Optional, informational" in a comment and is WARN-only: an invalid or missing signature (`$sig.Status -ne 'Valid'`) logs a warning and **does not halt installation**. The main `install-toast-agent.ps1` exits with code 3 on a bad signature; the template makes no such guarantee. | **FIXED:** Comment updated, WARN→ERROR log, `Remove-Item` + `exit 3` added in both the failure branch and the catch block — now matches main script enforcement. PS1 parse gate: OK. Abish sweep: SHIP. | High |
| XT-M1 | Medium | OPEN | `src/ToastRevival.Api/Controllers/DevicesController.cs` (carve-out, anchored) | **(Carried forward — unchanged since 2026-06-02.)** Single-use-token reinstall carve-out identifies "same machine" by the self-reported `(DeviceName, Username)` tuple — not hardware-backed. A compromised endpoint with a spent HKLM token value and knowledge of its own name/username could re-enroll a second device. | Defense-in-depth gap (HKLM read already implies machine compromise). **Decision (Keith, phone 2026-06-02):** fresh-token-per-reinstall REJECTED (breaks silent RMM mass deploy); fix = bind token to machine SID, scoped as build-mode project **XT-3**. **Owner: Keith.** Stays OPEN until XT-3 ships. | Medium |
| AA-M1 | Medium | OPEN | `TokenService.cs:57-60`; `AuthController.cs` Login/VerifyLoginSms/VerifyLoginTotp responses | `CreateRefreshToken()` returns 64 random bytes as a base64 string included in `AuthResponse` but never stored in the database. There is no token-exchange endpoint, no rotation, and no revocation path. The refresh token is decorative: clients storing it can never use it, but a future developer adding `/api/auth/refresh` without a revocation table would issue permanently valid tokens. | Permanently valid tokens with no revocation surface — the full threat materializes the moment someone adds a refresh endpoint. Removing `refreshToken` from `AuthResponse` entirely is safer than leaving a latent footgun in the API contract. | High |
| AA-M2 | Medium | OPEN | `AuthController.cs:526-563` (`VerifyLoginSms`); `AuthController.cs:681-726` (`MfaVerifySms`); `Program.cs` login-sms-per-ip policy: 5 attempts / 15 min | SMS OTP covers 1,000,000 values. An attacker distributing requests across ~200,000 IPs can exhaust the full 6-digit space in a single 15-minute window against a known `UserId`. The `UserId` is disclosed in the `LoginSmsChallenge` response after a successful password check, giving the attacker the required targeting key. | Full 6-digit OTP space exhausted in one rate-limit window via IP rotation. The challenge response leaks the `UserId` needed to target the request. Per-`UserId` lockout (3-5 attempts before invalidation) is the fix. | High |
| AA-M3 | Medium | OPEN | `Program.cs:43-47` (Identity password options); `UserDtos.cs` `InviteUserRequest` `MinLength(8)`; `AuthDtos.cs` `SetPasswordRequest` `MinLength(8)` | `RequireUppercase=false`, `RequireNonAlphanumeric=false`, `RequiredLength=8`. The password 'password1' satisfies all rules. MFA is not enforced by default (`RequireMfa` defaults to false) and is absent for accounts without a confirmed phone or TOTP secret — those accounts receive a JWT directly at login (line 519). | Weak password policy enables credential-stuffing and dictionary attacks on any account that has not yet enrolled MFA. All new accounts begin in single-factor state until the user voluntarily enrolls. | High |
| AA-M4 | Medium | OPEN | `AuthController.cs:511-519` — the "no phone confirmed" branch of Login | After a successful password check, if neither TOTP nor a confirmed phone is present, the code falls through to issuing a full JWT with no second factor. Accounts created via `SystemController.CreateTenant` with `InitialPassword` set and accounts via `InviteUserRequest` both start in this state — all new accounts begin single-factor. | Every newly created or invited account starts with MFA skippable. An `MfaEnrollmentRequired` flag forcing enrollment before a full session token is issued is the recommended fix. | High |
| AA-M5 | Medium | OPEN | `TokenService.cs:43-54` — `CreateDeviceToken`; `Program.cs` JWT `ClockSkew=Zero` | Device JWTs are issued with a 365-day expiry. Revocation requires a DB hit on every request (`IsDeviceRevoked`). The device token has no epoch/stamp claim analogous to the user `tokenEpoch`, so revocation cannot be cached. At fleet scale this creates per-request DB load with no cache path — every heartbeat, every ping, every notification acknowledge hits the DB for revocation. | Per-request DB revocation check with no caching path. A `DeviceSecurityStamp` claim mirroring the user `tokenEpoch` pattern would allow a short-TTL cache and eliminate per-heartbeat DB load at scale. | High |
| AA-M6 | Medium | OPEN | `SsoController.cs:217` | `Redirect($"{FrontendBase()}/sso/callback#token={Uri.EscapeDataString(jwt)}")` — the full JWT (potentially MFA-elevated) is embedded in the URL fragment. Fragments are stored in browser session history, readable by any JavaScript on the page including third-party scripts, and capturable by browser extensions. | Full account takeover if the fragment is read by any script on the callback page. MFA-elevated tokens in the fragment means a compromised extension gets step-up access. Fix: short-lived single-use server-side exchange code instead of JWT in the fragment. | High |
| AA-M7 | Medium | OPEN | `DevicesController.cs:719-737` — `PassesEnrollmentGateAsync` reinstall branch | When a single-use token is already consumed, re-registration is allowed if the presented token matches `UsedByDeviceName` and `UsedByUsername`. Both values are self-reported by the client. An attacker reading the token from HKLM plus knowing the original device name and Windows username can re-register the device identity under an attacker-controlled agent. | Self-reported identity allows an attacker with read access to HKLM (local admin or equivalent compromise) to hijack a device's enrollment identity. Severity bounded by requiring local admin access — hence Medium, not High. XT-3 (machine SID binding) is the scoped fix per Keith. | Medium |
| AA-M8 | Medium | OPEN | `BillingController.cs:76-97` — GET/POST `/api/billing/admin/stripe-config` | `POST /api/billing/admin/stripe-config` accepts the Stripe secret key and writes it to config. Protected by `IsPlatformAdmin()` but no `RequireFreshMfa()` guard, unlike parallel `SystemController` write endpoints. A compromised platform-admin session can rotate the Stripe secret key to an attacker-controlled value without requiring fresh MFA. | Stripe secret key rotation requires only a valid (potentially hours-old) platform-admin session — no fresh MFA step-up. An attacker with a stolen platform-admin token can redirect all billing to an attacker-controlled Stripe account. | High |
| AA-M9 | Medium | OPEN | `NotificationsController.cs:633-636` — `ResolveTargetDeviceIds` `TargetType.Device` branch | The device target branch queries `_db.Devices.Where(d => d.Status == Active && TargetIds.Contains(d.Id))` relying entirely on the global filter for tenant isolation. The `TargetType.Group` branch already has explicit `d.TenantId == tenantId` predicates, making the inconsistency notable. | Cross-tenant device targeting via the TargetType.Device path if global filter is bypassed. The inconsistency with `TargetType.Group` suggests the explicit predicate is known to be the correct pattern. | High |
| AA-M10 | Medium | OPEN | `UserDtos.cs:14-17` — `InviteUserRequest(Email, Password, Role)`; `UsersController.cs:70` | Admin-created users receive their initial password in the request body. This password appears in any request logging middleware, APM traces, or structured log sinks that capture request bodies. The invited user's credential is logged in plaintext before the user even knows they have an account. | Credential exposure in logs. Any logging system capturing request bodies (AWS CloudWatch, Datadog, Sentry breadcrumbs, nginx access logs) permanently stores the plaintext password. Remove `Password` from `InviteUserRequest` and use a set-password link flow instead. | High |
| INJ-M1 | Medium | OPEN | `TemplatesController.cs:65-70, 114-119`; `TemplateDtos.cs` (`CreateTemplateRequest` / `UpdateTemplateRequest`) | `HeroImageUrl` and `LogoImageUrl` in template DTOs carry no `[AbsoluteHttpUrl]` annotation and no content-moderation scan. `SendNotificationRequest` correctly applies both. A template with `file://`, `\\UNC\share\`, or `javascript:` URLs can be saved and then serialized into a signed notification payload reaching every enrolled Windows agent. | Malicious URLs baked into a reusable template propagate to every notification sent from that template — no per-send validation catches them. Supply-chain attack surface: compromise one template, blast to fleet. | High |
| INJ-M2 | Medium | OPEN | `DevicesController.cs:640-659` (`ToPublicUrl`) | When a stored asset URL is relative, `ToPublicUrl` builds an absolute URL by reading `X-Forwarded-Host` directly from raw request headers — not the `ForwardedHeaders`-validated `Request.Host`. An attacker with a device JWT can bake a hostile origin into `LogoUrl` and `LockScreenImageUrl` returned to the agent, creating a targeted SSRF/host-header injection. | SSRF and host-header injection via the asset URL returned to enrolled agents. A device-JWT holder can redirect all logo/lockscreen fetches to an attacker-controlled server across the tenant. | High |
| INJ-M3 | Medium | OPEN | `NotificationsController.cs:450-453, 471` | `EscapeCsvTitle` only doubles internal quotes — it does not prefix formula-trigger characters (`=`, `+`, `-`, `@`, `\t`, `\r`) with an apostrophe. The notification title is also written un-sanitized into the comment header line without quoting, so a title containing a comma or newline breaks the comment structure. | CSV formula injection. Any notification title beginning with `=`, `+`, `-`, or `@` executes as a formula in Excel/Google Sheets when the tenant exports delivery reports. | High |
| INJ-M4 | Medium | OPEN | `TemplatesController.cs:70, 119` | `ActionButtonsJson` is accepted as a raw string and written directly to the database without parsing, URL allow-list check, or button count limit. The notification send path passes `ActionButtons` through `TryNormalizeActionButtons`, which enforces a 3-button cap, validates label/actionId length, and enforces `http(s)` URLs. Templates bypass all these guards entirely. | Malformed or malicious `ActionButtonsJson` (arbitrary URLs, oversized labels, unlimited button count) stored in templates and delivered to the agent without any of the validation the direct send path applies. | High |
| INJ-M5 | Medium | OPEN | `Program.cs:427-432` | CSP `script-src` allows `'self'` without nonce or hash. If an attacker places any `.js` file anywhere under served static paths, that script executes in the SPA context. `img-src 'self' data: https:` allows any `https:` image URL including attacker-controlled ones. `'unsafe-inline'` on `style-src` is broader than necessary for an API. | Any file upload vulnerability or misconfigured static serve path becomes a full XSS vector. `'unsafe-inline'` on styles enables CSS-based data exfiltration attacks in some browser configurations. | Medium |
| DOS-M1 | Medium | OPEN | `CloudflareTurnstileVerifier.cs:39-48`; `AuthController.cs:82` | `CloudflareTurnstileVerifier` is registered as transient and re-reads config on each request scope. If `appsettings.Local.json` reloading blanks the keys at runtime, `IsEnabled` may flip to false without a restart. Behavior on misconfiguration is fail-open (not loudly signaled at boot) — a blank Turnstile key silently allows all traffic through the bot-protection gate. | Silent bot-protection bypass on misconfiguration. No startup validation throws if `Turnstile:SiteKey` or `Turnstile:SecretKey` are absent in non-Development environments. | Medium |
| DOS-M2 | Medium | OPEN | `NotificationQueueService.cs:22` | `Channel.CreateUnbounded`. `Enqueue()` calls `TryWrite` unconditionally and can never fail. A burst of large `TargetType.All` blasts piles up. No maximum queue depth and no shed mechanism. The health endpoint exposes `QueueDepth` but nothing alerts on it or triggers 429 responses to callers. | Unbounded memory growth under notification burst. A tenant with 5,000 devices sending a `TargetType.All` notification every minute can exhaust available heap with no external signal until the process crashes. | High |
| DOS-M3 | Medium | OPEN | `AuthController.cs:202-220` — `ForgotPassword` | When the email IS found, the code calls `GeneratePasswordResetTokenAsync` and `SendAsync` (Mailjet I/O). When NOT found, the path returns immediately with no added delay. The observable response-time difference can enumerate valid email addresses over many requests. | Email enumeration oracle via timing side-channel. An attacker can build a list of valid registered email addresses by comparing response times of `POST /api/auth/forgot-password`. | Medium |
| DOS-M4 | Medium | OPEN | `DevicesController.cs:62-64` — `POST /api/devices/register` | When `TenantId` is not found, the endpoint returns 404 'Tenant not found.' before checking the enrollment key. An unauthenticated attacker can enumerate valid tenant GUIDs by comparing 404 vs 403 responses from the anonymous registration endpoint. | Tenant GUID enumeration by unauthenticated callers. Valid tenant GUIDs enable targeted attacks against specific tenants — brute-force enrollment, timing attacks on tenant-scoped paths. | Medium |
| DOS-M5 | Medium | OPEN | `AuthController.cs:636-671` | `POST /api/auth/mfa/send-sms` has only per-IP limiting. An authenticated attacker rotating source IPs can trigger unlimited ClickSend SMS sends against any user's phone number with no per-`userId` or per-phone-number cap. | Unlimited SMS send cost amplification against any authenticated user's phone. Direct financial cost to operator (ClickSend per-message billing) plus harassment vector. | Medium |
| DC-M1 | Medium | OPEN | `Models/Enums.cs:10`; `BillingController.cs:355` | `public enum SubscriptionTier { Standard }` — a single-member enum. `BillingController` always sets `tenant.SubscriptionTier = SubscriptionTier.Standard`. No code path switches or branches on the value. The dashboard does not surface or inspect this field. | Dead enum creating false complexity. A future developer will assume `SubscriptionTier` drives behavior and search for its switch/if before discovering it does nothing. Remove from `Tenant`, drop column in migration, delete the enum. | High |
| DC-M2 | Medium | OPEN | `AuthContext.tsx:196-202` | `register()` function in `AuthContext` is defined but never called by any component. `Register.tsx` posts directly to `/api/auth/register/init` without touching `AuthContext`. The function calls an endpoint that returns `410 Gone` in production. | Dead code in `AuthContext` wrapping a permanently disabled endpoint. Future developer may attempt to use `useAuth().register()` and get a mysterious 410. | Medium |
| DC-M3 | Medium | OPEN | `Dashboard/src/api/moderation.ts:20`; `BlocklistDtos.cs` | TypeScript `BlocklistEntry` interface declares `createdByEmail?: string`. `Moderation.tsx` renders `b.createdByEmail ?? '—'`. The C# `BlocklistEntryResponse` has no creator email field. The column permanently shows '—' for every entry with no indication it is missing data. | Silent dead UI column. Users see an always-empty column and either assume no entries have a creator email or believe it is a display bug. | Medium |
| DC-M4 | Medium | OPEN | `src/ToastRevival.Api/Migrations/`; `src/ToastRevival.Api/Data/Migrations/` | Two migration folder trees exist: `Migrations/` (M1-M6 + `AppDbContextModelSnapshot`) and `Data/Migrations/` (M9A-M17, no `ModelSnapshot`). EF Core discovers both namespaces. The `AppDbContextModelSnapshot` in the old `Migrations/` namespace does not reflect M9A-M17 schema additions, making it misleading as a schema reference tool. | Dual migration trees with a stale snapshot create confusion for any developer running `dotnet ef migrations list` or using the snapshot for schema reference. EF Core may also produce unexpected behavior during scaffold operations. | High |
| ARCH-M1 | Medium | OPEN | `AuthController.cs`, `BillingController.cs`, `DeviceGroupsController.cs`, `DevicesController.cs`, `TenantController.cs`, `UsersController.cs`, `ModerationController.cs`, `BlocklistController.cs` | Each controller defines its own private `IsAdmin()`. Implementations differ: `DevicesController` checks three claim names; `BillingController` only parses `'role'`; `TenantController` uses `Enum.TryParse`; `UsersController` delegates to `GetCallerRole()`; `ModerationController` and `BlocklistController` do not check `platformAdmin`. A future policy change must be applied in eight places; the inconsistencies are a latent authorization bug surface. | Platform admins may not be recognized as admins on endpoints using narrow `IsAdmin()` implementations. A `ClaimsPrincipalExtensions.IsAdmin()` extension method is a one-file addition with one-line call-site changes. | High |
| ARCH-M2 | Medium | OPEN | `DevicesController.cs:614`; `NotificationsController.cs:614` | Full `IsDeviceRevoked()` method body — EF query, `.Select` projection, null/Decommissioned/TenantSuspended checks — is copy-pasted identically into both controllers with identical XML doc comments. Any change to suspension semantics must be applied twice; they will inevitably diverge. | Semantic drift between the two copies is when the bug appears. Moving to a shared `db.IsDeviceRevokedAsync(deviceId, ct)` extension eliminates the dual-maintenance burden. Check `NotificationHub.OnConnectedAsync` for a potential third copy. | Medium |
| ARCH-M3 | Medium | OPEN | `AuthController.cs` — 890 lines, 8 constructor dependencies | Handles: legacy direct-register, trial-request three-step flow, login with password, SMS MFA login verify, TOTP MFA login verify, MFA status, TOTP enroll/confirm/disable, SMS step-up send/verify. Trial registration helpers (`NormalizeWebsite`, `SlugifyTenantName`, `NormalizeSubdomain`, `RandomSuffix`) have nothing to do with authentication. | 890-line controller is difficult to test and maintain. Helpers unrelated to auth pollute the class. Split into at minimum `AuthController` (login, MFA verify, token concerns) and `RegistrationController` (trial flow, set-password). | Medium |
| ARCH-M4 | Medium | OPEN | `TenantController.cs` — 607 lines | Manages: general settings, enrollment key regeneration, MFA policy, content moderation settings, desktop overlay config, lock screen config and upload, and SSO. The `RequireStepUpMfa()` helper is defined here but inlined differently in `UploadLockScreenImage`, creating an inconsistency. | 607-line controller with eight distinct concerns. Split by concern: `AppearanceController` (overlay, lockscreen, logo), move moderation to `ModerationController`, move SSO mapping to `SsoController`. Core `TenantController` retains only settings and MFA policy. | Medium |
| ARCH-M5 | Medium | OPEN | `Dashboard/src/pages/Compose.tsx` — 1053 lines | Contains: Compose page (~270 lines, 20+ `useState` calls), `DeviceMultiSelect`, `GroupMultiSelect`, `AssetPickerModal` (~130 lines), `ChevronIcon`, and standalone button validation/normalization logic. `DeviceMultiSelect` and `GroupMultiSelect` share an identical search-input+scrollable-checklist pattern written separately. | 1053-line file with five embedded concerns. The two multi-select components are structural duplicates. Extracting `AssetPickerModal`, a shared `MultiSelectList`, and button logic to `lib/actionButtons.ts` drops the page to ~300 lines. | Medium |
| MT-M1 | Medium | OPEN | `Models/DeviceGroupMember.cs`; `AppDbContext.cs:65-80` | Global query filter is `m.DeviceGroup.TenantId == _tenantProvider.TenantId`, requiring a JOIN against `DeviceGroups` for every filtered query. If a `DeviceGroupMember` row references a `DeviceGroupId` pointing to a group with no matching tenant record, the filter may resolve unexpectedly. No direct row-level `TenantId` discriminator exists. | Navigation-join-based filter is less robust than a direct row-level predicate. Orphaned `DeviceGroupMember` rows (group deleted) may bypass or corrupt the filter in edge cases. Adding a `TenantId` column provides a direct discriminator independent of join integrity. | Medium |
| MT-M2 | Medium | OPEN | `AppDbContext.cs:129-135`; `AuditController.cs:44-64` | `AuditLog` intentionally has no EF Core global filter. `AuditController` correctly adds `l.TenantId == tenantId`. However, any future endpoint querying `_db.AuditLogs` without an explicit predicate silently exposes cross-tenant audit history. In `SystemController.RejectTrialRequest`, the audit log call uses `GetTenantId()` — the platform admin's own tenant GUID — not the trial requester's tenant, misattributing the record. | No filter safety net on `AuditLog` means one missing predicate in a future endpoint = cross-tenant audit exposure. The `RejectTrialRequest` misattribution means platform admin actions appear in the admin's own audit log, not associated with the target trial request. | Medium |
| MT-M3 | Medium | OPEN | `SystemController.cs:211-219` | `RejectTrialRequest` audit log entry is logged with `GetTenantId()` resolving to the platform admin's own tenant GUID. For a rejected trial there is no created tenant, so the audit record is misattributed to the admin's tenant audit log instead of a platform-level sentinel. | Cross-tenant platform operation records land in the wrong tenant's audit log. Compliance and forensic audit trails are unreliable for platform-admin actions on rejected trials. | Medium |
| MT-M4 | Medium | OPEN | `BlocklistController.cs:54` | `_db.TenantBlocklistEntries.AnyAsync(b => b.Term == term)` relies solely on the global filter for the duplicate-term check. If the global filter is misconfigured (null `TenantId`), this check always returns false, bypassing the duplicate guard and allowing the insert to hit the unique index with an unhandled `DbUpdateException`. | Silent duplicate-check bypass on filter misconfiguration, producing an unhandled exception rather than a clean 409. The explicit predicate `&& b.TenantId == tenantId` makes this robust against filter failure. | Medium |
| MT-M5 | Medium | OPEN | `NotificationsController.cs:422-428` | `DeliveryReport` fetches the parent `Notification` with an explicit `notif.TenantId == tenantId` predicate but then queries `NotificationDeliveries` with only `.Where(d => d.NotificationId == id)`, relying on the global filter for the delivery rows. | Inconsistency: parent record has explicit predicate, child records do not. A filter bypass exposes delivery records (device IDs, delivery timestamps, status) for notifications from other tenants. | Medium |
| MT-M6 | Medium | OPEN | `Models/Tenant.cs:15-19`; `DevicesController.cs:164` | Per-tenant HMAC-SHA256 signing key is returned to every registering device. No reset/rotate endpoint exists. If any single enrolled device is compromised and the signing key is extracted from its `config.json` (DPAPI-protected but CurrentUser-scoped), the attacker can forge HMAC-valid notification payloads for the entire tenant. No rotation mechanism is present by design. | Compromise of one device potentially compromises notification authenticity for all devices in the tenant. No rotation endpoint means the signing key is permanent until all devices re-register. Document as accepted architectural risk; plan a rotation endpoint. | Medium |
| SEO-M1 | Medium | OPEN | `scripts/prerender-seo.mjs:251-343` (docs sub-pages) | `/docs/deploy/store`, `/docs/deploy/intune`, `/docs/deploy/rmm`, `/docs/api` have thin prerendered bodies — one or two short paragraphs with no code examples, no step lists, no platform-specific detail. Googlebot's initial crawl indexes these thin bodies; full rich content is only available after JavaScript executes. | Thin prerendered content = poor SERP ranking for deployment-specific queries. MSPs searching for "Toast Notification RMM install" or "Intune deploy guide" hit thin placeholder pages instead of rich instructional content. | High |
| SEO-M2 | Medium | OPEN | `Pricing.tsx:122-159`; `prerender-seo.mjs:162-185` | Nine FAQ entries covering pricing mechanics, device counting, RDS sessions, cancellation, and data storage exist — exactly the content that earns FAQ rich results and AI assistant answers. The `useSeo` call uses only `BreadcrumbList`. The `pricingProductLd()` helper is defined in `seo.ts` but never invoked from any page. | Missed FAQ rich results and Product schema in Google SERPs. FAQ entries are among the highest-value structured data for converting search intent to direct traffic. `pricingProductLd()` was built but never wired up. | High |
| SEO-M3 | Medium | OPEN | `lib/seo.ts:209-232`; `prerender-seo.mjs:112` | `techArticleLd()` does not emit `datePublished` or `dateModified`. The prerender script's `articleLd()` adds `dateModified` but the runtime `useSeo()` hook — which replaces static JSON-LD after React hydrates — injects a `TechArticle` without either date field. Dates visible in prerendered HTML disappear after hydration. | Missing `datePublished`/`dateModified` on docs articles degrades Google's freshness scoring and removes E-E-A-T date signals. Post-hydration the static prerendered dates vanish, so crawlers see no dates on revisit. | Medium |
| SEO-M4 | Medium | OPEN | `lib/seo.ts`; `Home.tsx:35` | Home page uses `softwareApplicationLd()` which embeds a nested publisher of type `Organization`, but there is no standalone `Organization` schema block with full fields for Google Knowledge Panel: `@type Organization`, `name`, `url`, `logo` (ImageObject), `contactPoint`, `sameAs` (GitHub, Microsoft Store, etc.). | Without a standalone `Organization` schema, Google cannot reliably build a Knowledge Panel for Toast2IT. The nested publisher object in `softwareApplicationLd` is insufficient for Knowledge Graph entity establishment. | Medium |
| SEO-M5 | Medium | OPEN | `lib/seo.ts:87-94`; `index.html:27-38` | `useSeo` and static `index.html` set `og:image`, `og:image:width`, `og:image:height` but not `og:image:alt` or `twitter:image:alt`. Degrades accessibility of social shares and reduces link preview richness on platforms that use alt text for AI image understanding. | Missing social image alt text fails WCAG accessibility for social card previews and reduces AI/crawler understanding of shared images. One-line addition per social image tag. | Medium |
| REST-M1 | Medium | OPEN | `Program.cs:436-440` | `UseSwagger()`/`UseSwaggerUI()` wrapped in `if (app.Environment.IsDevelopment())`. Production deployments expose no machine-readable API contract. Partner integrations, the Windows agent team, and any future SDK generation have no canonical schema. | No production API documentation. External integrators, the agent team making API calls, and AI coding assistants cannot auto-discover the API schema. Run Swashbuckle CLI at build time and publish the artifact at minimum. | High |
| REST-M2 | Medium | OPEN | All controllers — `[Route("api/[controller]")]` with no version segment | Every controller uses unversioned routes. The Windows agent (MSI-deployed, slow-rollout) and the dashboard are both coupled to the same unversioned surface. Breaking changes cannot be introduced without breaking all currently-deployed agent versions simultaneously, forcing coordinated releases across the fleet. | Zero API versioning means any breaking change requires full fleet coordination. Agent version N-1 must continue working against the API while N rolls out — currently impossible to guarantee without versioning. | High |
| REST-M3 | Medium | OPEN | `Program.cs` (middleware pipeline) | No middleware emits a request correlation ID. Structured logs contain no correlation handle that the dashboard or MSP could match against a server-side log entry. No `X-Request-Id` header on any response. | Debugging production issues requires correlating client errors with server logs by timestamp only — slow and imprecise. Adding `Activity.Current?.TraceId` echoed as `X-Request-Id` is a one-line middleware addition. | Medium |
| REST-M4 | Medium | OPEN | `AuthController.cs`, `BillingController.cs`, `UsersController.cs`, `DevicesController.cs`, `SystemController.cs` | At least four distinct error shapes: (1) raw string — `BadRequest("Tenant name...")`; (2) anonymous with `message`; (3) anonymous with `error` + `message`; (4) anonymous with `errors` array; plus ASP.NET Core's default model validation ProblemDetails. `client.ts:43-69` works around this with a multi-branch extractor. | Four error shapes means client-side error parsing is a fragile multi-branch mess. Any new endpoint can silently introduce a fifth shape. RFC 9457 ProblemDetails via `builder.Services.AddProblemDetails()` standardizes this platform-wide. | High |
| REST-M5 | Medium | OPEN | `NotificationsController.cs:243-267`; `AuditController.cs:34-66` | `GET /api/notifications` accepts `page`/`pageSize` but returns a bare array. `GET /api/audit` similarly. The frontend has no way to render accurate pagination controls or determine whether more pages exist — it cannot distinguish "page 3 of 3" from "page 3 of 300". | Blind pagination breaks the UI. Users on page 3 of a large history set cannot know if they've seen all records. `{ items, totalCount, page, pageSize }` envelope or `X-Total-Count` header resolves this. | High |
| REST-M6 | Medium | OPEN | `DevicesController.cs:167-180` | `GET /api/devices` fetches every non-decommissioned device for the tenant with no `page`/`limit` parameter. Also includes `GroupMemberships` via eager loading. A tenant with thousands of devices returns a multi-megabyte payload on every dashboard load and every Compose mount. | Unbounded payload on every dashboard load. React Devices page and Compose page both call this endpoint on mount. At 1,000+ devices, significant payload and parse time on every navigation. | High |
| PERF-M1 | Medium | OPEN | `AuditService.cs:18-33` | Every audit event creates a new `IServiceScope`, resolves `AppDbContext`, adds one row, and calls `SaveChangesAsync` — a dedicated DB round-trip per log line. Endpoints emitting multiple audit events in a single request pay this cost serially. High-write endpoints (bulk device operations) serialize audit flushes. | N+1 DB round-trips for audit logging. A `Channel<AuditLog>` buffer with timer-based flush (2-second window or 50-row threshold) eliminates the per-event round-trip cost. | Medium |
| PERF-M2 | Medium | OPEN | `NotificationQueueService.cs:21` — `Channel.CreateUnbounded` | Consumer is single-threaded (`SingleReader=true`, `ProcessQueueAsync` runs sequentially). Large fanouts per notification compound the backlog with no mechanism to signal callers or shed load. `Enqueue` always succeeds regardless of consumer throughput. | Single-reader sequential processing cannot keep pace with large notification bursts. Switching to `Channel.CreateBounded` with a capacity cap and returning 503 on `TryWrite` failure provides backpressure to callers. | Medium |
| PERF-M3 | Medium | OPEN | `NotificationQueueService.cs:153-170` — `RecoverOrphansAsync` | `.ToListAsync()` materializes complete `Notification` rows (including `Title`, `BodyLine1`, `HeroImageUrl`, `ModerationResultJson`, etc.) just to write two columns (`Status` and `CompletedAt`). Every property of every orphaned notification is loaded into memory for a pure-update operation. | Unnecessary full-entity hydration on startup recovery. `ExecuteUpdateAsync` handles this in a single `UPDATE` SQL statement with no entity materialization — a one-line fix. | Medium |
| PERF-M4 | Medium | OPEN | `NotificationsController.cs:68-148` — `Send()` | Two separate sequential DB reads to the `Tenants` table: first for `{RequireMfa, Suspended}` (line 69), then again for `{ModerationRequireApprovalAll, ModerationBlockedMessage}` (line 141). Two round-trips where a single projection of all four columns would suffice. | Two Tenants table reads per notification send. At high send volume this doubles the DB load on the most performance-critical endpoint path. Consolidate into one projection. | Medium |
| PERF-M5 | Medium | OPEN | `DevicesController.cs:392-394` — `Ping()` | `.Include(d => d.Tenant)` eagerly loads the entire `Tenant` row (~30 columns) just to check `device.Tenant?.SuspendedAt`. Devices call this on a schedule; at fleet scale this generates a JOIN against a wide table on every heartbeat tick. | Wide JOIN on every device heartbeat. At 500 devices pinging every 60 seconds, this is 500 full Tenant row loads per minute instead of one projected boolean per device. Project only `SuspendedAt` or use a `Select` to avoid the join entirely. | Medium |
| REL-M1 | Medium | OPEN | `BillingController.cs:158, 192, 219`; `StripeBillingSyncService.cs:32-44` | All Stripe SDK calls are bare awaits with no retry. Stripe SDK does not retry by default. A transient error on checkout or portal creation returns a 500 to the user. `BillingController.CreateCheckout` and `CreatePortal` have no `try/catch` at all — any `StripeException` propagates to Kestrel and surfaces as a raw 500. | Transient Stripe errors cause hard billing failures for users attempting to upgrade or manage their subscription. No retry means a momentary Stripe API blip turns into a failed customer transaction with no self-healing. | Medium |
| REL-M2 | Medium | OPEN | `MailjetEmailService.cs`, `ClickSendSmsService.cs`, `CloudflareTurnstileVerifier.cs`, `ContentSafetyService.cs`, `MicrosoftSsoService.cs` | None of the five external HTTP integrations have a circuit breaker. A prolonged provider failure causes every request on that path to wait for the full HTTP timeout (default 100s), blocking thread pool threads. Turnstile failure blocks all registrations. Content Safety failure blocks all notification sends with moderation enabled. | Five independent outage vectors, each capable of exhausting the thread pool or starving other request paths. Polly circuit breaker (or `Microsoft.Extensions.Http.Resilience`) on all five `HttpClient` registrations is the fix. | High |
| REL-M3 | Medium | OPEN | `ContentSafetyService.cs:44-58`; `Program.cs:312` | `ConcurrentDictionary<string, ContentSafetyClient>` is a static field — `ContentSafetyClient` instances are never evicted. If a tenant rotates their `ModerationCustomKey`, the old client stays in memory forever alongside the new one. Under heavy tenant churn or frequent key rotation, this is a slow memory leak with no eviction path. | Unbounded static cache grows indefinitely with tenant count and key rotation frequency. `IMemoryCache<string, ContentSafetyClient>` with sliding expiry (e.g., 1 hour) allows stale entries to be evicted after key rotation. | Medium |
| REL-M4 | Medium | OPEN | `UpdateService.cs:73-76` | Catch block is `catch { /* fall through to default */ }` — a bare catch swallowing all exceptions including `OutOfMemoryException` and `ThreadAbortException`. A registry ACL denial or corrupted value will never surface in `DiagLog`, making it invisible in field diagnostics. | Completely silent exception swallowing on registry reads. Operational debugging of update failures requires visible error traces. Replace with `catch (Exception ex)` and a `DiagLog.Write` line matching the pattern used in `IsAutoUpdateEnabled`. | Medium |
| REL-M5 | Medium | OPEN | `Dashboard/src/App.tsx`; `Dashboard/src/main.tsx` | The entire React tree has no `ErrorBoundary`. A runtime JavaScript error in any component (null-deref on unexpected API shape, failed lazy import, Recharts rendering error) unmounts the entire application and displays a blank white screen. | An uncaught JS error in any component — including a minor rendering issue in Analytics — blanks the entire dashboard. One `ErrorBoundary` wrapping `RouterProvider` with a "Something went wrong — refresh" fallback prevents total application failure. | High |
| DEVOPS-M1 | Medium | OPEN | `src/ToastRevival.Api/appsettings.json:37` | `Sso.Microsoft.ClientId = bfef46d4-5c86-4da2-a630-58c6b06d44ce` committed in plain text. Pins the application to a specific Entra app registration, cannot be varied per-environment without a code change, and for self-hosted deployments leaks Toast2IT's own Entra tenant configuration. | Committed `ClientId` is not a secret but exposes Toast2IT's Entra app registration identity publicly in the mirror repo. Self-hosted customers must modify source to use their own SSO registration. Move to `docker-compose.yml` env var with `.env.example` documentation. | Medium |
| DEVOPS-M2 | Medium | OPEN | `src/ToastRevival.Api/appsettings.json:3-4` | Base `appsettings.json` ships with canonical PostgreSQL default credentials (`postgres`/`postgres`). `docker-compose.yml` correctly overrides via `${POSTGRES_PASSWORD}`, but a developer running `dotnet run` locally without `appsettings.Local.json` set will attempt connection with default credentials and may succeed against a locally-installed Postgres instance. | Default credentials in source make it easy for developers to accidentally connect to a real database using the canonical bad-practice credentials. Replace with a clearly inert placeholder and add a startup guard in non-Development. | Medium |
| DEVOPS-M3 | Medium | OPEN | `scripts/sync-public-mirror.ps1:68` | Unlike every other script setting `ErrorActionPreference = Stop`, this script deliberately sets it to `Continue`. Cmdlet-level failures (file permission errors during copy, encoding failures in `Set-Content`) produce a warning and continue, potentially publishing a partially sanitized or partially copied mirror without any error signal. | A partially sanitized mirror could expose internal-only content or credentials if `Set-Content` silently fails mid-sanitization. `$ErrorActionPreference = "Stop"` is the fix; capture git stderr with `2>$null` on individual calls rather than lowering the global error preference. | High |
| DEVOPS-M4 | Medium | OPEN | `docker-compose.yml:72` | Compose stack maps the dashboard nginx container to host port 80 with no HTTPS mapping and no TLS terminator. The `nginx.conf` comment acknowledges this. A self-hosted deployer running `docker compose up -d` as-is serves the entire application over unencrypted HTTP — JWTs, MFA codes, tenant data, and device credentials in the clear. | Self-hosted deployments ship credentials in plaintext by default. Adding a documented TLS option (Caddy or Traefik with automatic Let's Encrypt) directly to `docker-compose.yml` prevents this being a footgun for self-hosted customers. | High |
| DEVOPS-M5 | Medium | OPEN | `docker-compose.yml`; `Program.cs` | All production secrets (JWT key, Stripe keys, Turnstile, content safety, email/SMS API keys) are plain env vars sourced from a `.env` file on disk. No integration with Azure Key Vault, AWS Secrets Manager, or similar. Any process in the container can read all secrets via `/proc/1/environ`. | No secrets management beyond plain env vars. Restrict `.env` to `600` permissions at minimum. Long-term: integrate AWS Secrets Manager via `Amazon.Extensions.Configuration.SystemsManager` to eliminate plaintext secrets on disk entirely. | Medium |
| WSEC-M1 | Medium | OPEN | `TenantLogoStore.cs:70-77` (`DownloadAsync` / `ResolveExtension`) | File extension derived solely from URL path segment. No `Content-Type` header check and no magic-byte validation. A tenant-controlled response could deliver any byte sequence (SVG with embedded script, XML, PE binary) while the URL ends in `.png`. `ReadAsByteArrayAsync` fully buffers the download before the size check — a slow-drip server can consume up to 4 MB of heap before the check fires. | Malicious file delivery via logo URL. A compromised or attacker-controlled image host can deliver executable content disguised as an image to the agent. Check `Content-Type` response header against an allowlist and validate magic bytes before processing. | High |
| WSEC-M2 | Medium | OPEN | `UpdateService.cs:67-77` (`GetFeedUrl`) | The registry value is returned without validating the scheme. An `http://` URL in the registry creates a downgrade-to-cleartext vector — an on-path attacker can substitute a malicious update package. The MSI path (`SelfUpdateService.DownloadAndVerifyMsiAsync`) has a correct `DEBUG/RELEASE` guard rejecting `http://` in production; `UpdateService.GetFeedUrl` has no equivalent guard. | HTTP downgrade attack on the Velopack update feed. An on-path attacker (or a local attacker who can write to HKLM) can redirect the agent to an HTTP feed and substitute an unsigned update package. | High |
| WSEC-M3 | Medium | OPEN | `DeviceConfig.cs:20`; `ConfigStore.cs:98-99` (DPAPI protect/unprotect) | HMAC signing key stored in `config.json` with DPAPI `CurrentUser` scope. Any process running in the same Windows session under the same OS user account can decrypt `config.json` and extract the signing key. Once extracted, the attacker can forge HMAC-valid notification payloads for the entire tenant. | DPAPI `CurrentUser` scope provides no protection against other processes running as the same user — the toast agent's user account is the attack surface. If the interactive user has admin rights, any admin process can extract the key. | Medium |
| WSEC-M4 | Medium | OPEN | `AgentClient.cs:28, 83, 130, 260`; `SelfUpdateService.cs:522`; `TenantLogoStore.cs:52` | Every `HttpClient` in the agent uses the default `HttpClientHandler` without certificate pinning. In MSP/enterprise environments, corporate SSL inspection proxies (Zscaler, Palo Alto, etc.) installed as trusted roots intercept all agent connections transparently. An SSL inspection proxy can observe HMAC signing keys received at registration and all notification payloads. | Corporate SSL inspection is standard in MSP environments — the exact deployment target for Toast Notification. SPKI hash pinning for registration and hub connections would prevent transparent interception even from trusted-root proxies. | Medium |
| WSEC-M5 | Medium | OPEN | `AgentClient.cs:752-790` (`ToastUrlLauncher.OpenIfAllowed`) | Scheme restriction to `http`/`https` is correct. However, `UseShellExecute = true` invokes whatever application is registered as the default http/https handler. On endpoints where a vulnerable browser version or a malicious application has registered as the http handler, the agent becomes a delivery mechanism. URL is derived from server-controlled (HMAC-signed) notification payload. | Agent as URL delivery vector via whatever is registered as the OS http handler. On non-standard MSP environments, the default handler may not be a mainstream browser. An allowlist of permitted hostname suffixes per-tenant adds a meaningful defense layer. | Medium | Low | FIXED-VERIFIED | `src/ToastRevival.Agent/AgentClient.cs:42,735` | `HttpResponseMessage` objects returned by `PostAsJsonAsync` at lines 42 and 735 are not wrapped in `using var`. Every other HTTP call in the file uses `using var resp = ...` (lines 91, 137, 484, 666, 692). After `ReadAsStringAsync` / `ReadFromJsonAsync` drains the body, the socket is effectively returned, but deterministic disposal of the message object (headers, content buffers) is skipped and deferred to GC. | **FIXED:** `using var` added at both sites (line 42 `response`, line 735 `resp`). C# build: succeeded, 0 warnings. Abish sweep: SHIP. | High |

| AA-L1 | Low | OPEN | `SystemController.cs`: GET `trial-requests`, `tenants`, `tenants/{id}`, `billing-overview`, `users`, `devices` | `RequireFreshMfa()` is correctly applied to all mutating platform-admin endpoints but not to read endpoints that expose full cross-tenant user PII (email, full name, role, MFA status), tenant financial data (Stripe IDs, MRR), and device inventory. A stolen platform-admin session token (up to 8 hours old per DEVOPS-H1) is sufficient to read all tenant PII without re-authenticating. | Cross-tenant PII exposure to stale session tokens. Apply `RequireFreshMfa()` to GET `tenants/{id}`, GET `users`, and GET `billing-overview` at minimum. | Medium |
| AA-L2 | Low | OPEN | `AuthController.cs:135-165` — `VerifySms` (registration flow) | The login SMS verify path correctly calls `RegisterFailedSmsAttemptAsync` on wrong code. The registration counterpart returns `Unauthorized` on wrong code but does not increment the lockout counter. An attacker can brute-force the 6-digit registration OTP without triggering lockout — only the per-IP rate limit applies. | Registration OTP brute-forceable without triggering lockout. Pairs with DOS-H2 (no rate limit at all) to create a fully unguarded path. | High |
| AA-L3 | Low | OPEN | `Program.cs:104-114` — `TokenValidationParameters`; `TokenService.cs:93` — `HmacSha256` | `TokenValidationParameters` does not set `ValidAlgorithms`. The library defaults to accepting any algorithm in the token header. With a symmetric key this is lower risk, but an explicit pin (`ValidAlgorithms = new[] { SecurityAlgorithms.HmacSha256 }`) is RFC 8725 best practice and costs nothing. | Without algorithm pinning, a future library change or algorithm confusion attack (less likely with symmetric keys but non-zero) could accept tokens signed with an unexpected algorithm. | Low |
| AA-L4 | Low | OPEN | `Program.cs:342-348` — CORS configuration | Development CORS allows any origin with credentials. A misconfigured `ASPNETCORE_ENVIRONMENT` set to `Staging` or any non-Production value in a live environment activates the wildcard policy. No startup validation exists to catch this. | Wildcard CORS with credentials in production = cross-site credential theft. Add a startup validation in non-Development environments throwing `InvalidOperationException` when `AllowedOrigins` is null or empty. | Low |
| INJ-L1 | Low | OPEN | `DeviceDtos.cs:5-11` (`RegisterDeviceRequest`); `DevicesController.cs:59-165` | Both `DeviceName` and `Username` carry `[Required]` but no `[MaxLength]`. The registration endpoint is anonymous. Multi-MB device names can consume database space and disrupt CSV export formatting or dashboard rendering. | Unbounded field length on an anonymous endpoint. Add `[MaxLength(256)]` to both fields and trim both in the `Register` action before the idempotent lookup. | Low |
| INJ-L2 | Low | OPEN | `SsoController.cs:217` (injection angle) | Full JWT in URL fragment is accessible to JavaScript on the callback page. If the React SPA reads `location.hash` before clearing it, third-party scripts can observe it. Fragment not cleared by `history.replaceState` call. | JWT visible to all JavaScript on the callback page. Even if the short-lived code exchange (from AA-M6) is implemented, ensure `/sso/callback` calls `history.replaceState('')` immediately after reading the fragment. | Low |
| INJ-L3 | Low | OPEN | `DevicesController.cs:719-737` (injection angle) | `DeviceName` and `Username` are both self-reported. No hardware binding. Log a warning-level audit event whenever the carve-out path is taken — until XT-3 (machine SID binding) ships, this provides a detection signal without changing behavior. | No audit trail when the reinstall carve-out fires. Adding a warning audit log event enables detection of potential token-replay attacks without breaking RMM mass-deploy workflows. | Low |
| INJ-L4 | Low | OPEN | `client.ts:7-8` (`getToken` / `localStorage`) | JWT stored in `localStorage` is directly accessible to any JavaScript on the origin. React JSX escaping is a strong default guard against XSS, but `localStorage` is the first thing an XSS payload reads. | `localStorage` token storage means any XSS = full session compromise. Evaluate `HttpOnly` cookie-based storage with a BFF pattern as a future hardening milestone. Ensure no `dangerouslySetInnerHTML` in the SPA (confirmed clean per 2026-06-03 pass). | Low |
| DOS-L1 | Low | OPEN | `AuditController.cs:70-107` — GET `/api/audit/export` | Fetches the FULL audit log for up to 90 days with no `Take`/limit. For a high-volume tenant this could be tens of thousands of rows materialized into memory and serialized into a QuestPDF PDF or CSV. No maximum row count and no truncation header if the cap were hit. | Uncapped export materializes unlimited rows into process memory. Apply a maximum row count cap (e.g., 50,000 rows) via `.Take()` before materializing. Return a truncation header (`X-Truncated: true`) if the cap is hit. | Medium |
| DOS-L2 | Low | OPEN | `BillingController.cs:242-292` | `[DisableRateLimiting]` and `[AllowAnonymous]` required for Stripe delivery. Body is read via `StreamReader` with no explicit size limit before signature verification. A large forged POST can consume significant server I/O and memory before being rejected. | Unbounded request body read before Stripe signature verification. Add `[RequestSizeLimit(65536)]` (64 KB is sufficient for any Stripe event payload). Stripe signature verification happens after the full body is read. | Low |
| DOS-L3 | Low | OPEN | `HealthController.cs` — GET `/api/health` | Response includes `queueDepth`, `uptimeSeconds`, and `db.latencyMs`. These collectively help an attacker fingerprint deployment state, time targeted requests during queue saturation, or identify post-restart windows where queues are empty. | Infrastructure telemetry exposed to unauthenticated callers. Move detailed subsystem metrics behind an authenticated endpoint or a configurable secret token header. Expose only binary healthy/unhealthy to public probes. | Low |
| DC-L1 | Low | OPEN | `MfaService.cs:78-89` | `MfaService.Verify()` (non-atomic overload) is documented as "NOT safe to use as the production replay guard." No controller or service calls it in production. Public visibility could mislead future developers into using it where `VerifyAndClaimAsync` is required — the replay-safe version. | A future developer using `Verify()` instead of `VerifyAndClaimAsync` breaks the OTP replay protection entirely with no compiler warning. Make `internal` and add `[assembly: InternalsVisibleTo]` for test access, or delete if not test-referenced. | Medium |
| DC-L2 | Low | OPEN | `scripts/generate-toast-assets.ps1` | Mentioned in M0A spike docs but never called by `build-msi.ps1`, `build-msix.ps1`, `build-release.ps1`, or `build.ps1`. One-time bootstrap helper no longer necessary once real assets are committed. | Orphaned script creates confusion about whether it should be called in the build pipeline. If the agent Assets directory already contains correct brand PNGs as committed files, delete it. | Low |
| DC-L3 | Low | OPEN | `scripts/publish-agent.ps1` | A bare `dotnet publish` wrapper. Both `build-msi.ps1` and `build-release.ps1` perform the same step internally. No documentation, README, CI pipeline, or other script references it. | Superseded by two other scripts. Leaving it implies it has a distinct purpose it doesn't have. Delete. | Low |
| DC-L4 | Low | OPEN | `scripts/run-agent-spike.ps1` | M0A spike testing helper. All references are in old spike evidence docs. No CI pipeline or current workflow documentation references this script. | Development-only diagnostic with no production or CI role. Delete or move to a dev-tools directory with a clear header comment noting when it was last used. | Low |
| DC-L5 | Low | OPEN | `AuthController.cs:335`; `appsettings.json:23` | `POST /api/auth/register` always returns `410 Gone`. No dashboard UI, `SystemController` endpoint, or documentation path exists to toggle `Registration:AllowLegacyDirectRegister`. The `Register()` action and `RegisterRequest` DTO exist solely to serve a dead code path with a hardcoded Gone response. | Dead controller action + DTO + config key. If the path is permanently retired, delete `Register()`, `RegisterRequest`, the config key, and `authApi.register()` in the frontend client. | Low |
| ARCH-L1 | Low | OPEN | `BillingConfigService.cs:93`, `MessagingConfigService.cs:90`, `SsoConfigService.cs:90` | All three runtime-config services contain an exact copy of `ReadOrCreateRoot()`. The `FileLock` pattern, `ConfigurationRoot.Reload()` call, and `JsonOptions` constant are also duplicated. Three separate `FileLock` objects currently allow concurrent writes across the three services to race on the same physical file. | Triplicated code with a latent concurrent-write race. A shared `LocalSettingsStore` helper with `ReadOrCreateRoot()`, `WriteRoot()`, and one `FileLock` eliminates both the duplication and the race. | Low |
| ARCH-L2 | Low | OPEN | `AuthController.cs:722, 806, 883` | Three action methods each inline `int.TryParse(_config["Jwt:MfaElevationExpiresInMinutes"], out var m) ? m : 15`. The controller already has a `SessionExpiresAt()` helper for user-token lifetime — a `MfaExpiresAt()` parallel method is the obvious fix. | Three inline config parses with a fallback default, one of which could silently use the wrong value if the config key name is ever changed. Extract once to `MfaExpiresAt()`. | Low |
| ARCH-L3 | Low | OPEN | `Dashboard/src/pages/Devices.tsx` — 900 lines | Contains device list, filter/sort toolbar, group management panel, bulk-selection toolbar, and a full `DeviceGroupModal` (~160 lines). The page has 14 `useState` calls. `formatRelative`/`formatDate` are inlined rather than in a shared `lib/dateFormat.ts`. | 900-line page with an embedded 160-line modal. Extract `DeviceGroupModal` to `components/DeviceGroupModal.tsx`. Move date formatters to `lib/dateFormat.ts`. | Low |
| ARCH-L4 | Low | OPEN | `TenantController.cs:562` and `:584` | `NormalizeLogoUrlForStorage` and `NormalizeLockScreenUrlForStorage` are structurally identical — same operation with different prefix constants (`/assets/logos/` vs `/assets/lockscreen/`). Code body is copy-pasted. | Two-line private methods with identical bodies. Extract `private static string? NormalizeAssetUrlForStorage(string? url, string prefix)` and call from both sites. | Low |
| ARCH-L5 | Low | OPEN | `Compose.tsx` — lines ~350, 395, 460, 508, 519, 530, 562, 572, 581, 790, 845 | Every text/select/url input repeats the same inline style object with ~6 identical properties. Same pattern in `DeviceGroupModal` and the multi-select search boxes — approximately 10 repetitions. | ~40 lines of duplicated inline style that should be an `input-dark` CSS class expressed once. One-time extraction. | Low |
| ARCH-L6 | Low | OPEN | `ContentSafetyService.cs:60-143` | `NotificationsController.Send` calls `ModerateTextAsync` then `ModerateImageUrlAsync`. Each call independently calls `GetPolicyAsync` which does a DB SELECT on the Tenants table. Two identical DB reads of the same tenant row per notification send, plus two separate `ContentSafetyClient` lookups from the static cache. | Two redundant DB reads per notification send on the moderation code path. Cache the policy in an instance field after the first call (scope as `Scoped`) or add an overload accepting the policy as a parameter. | Low |
| MT-L1 | Low | OPEN | `Models/TrialRequest.cs`; `AppDbContext.cs:185-201` | `TrialRequest` is a platform-level entity (pre-tenant-creation) correctly having no global query filter. But the absence is visually inconsistent with all other entities and could confuse future developers who see no filter and assume one is missing. | Add a code comment in `AppDbContext` next to the `TrialRequest` entity configuration stating the filter is intentionally absent and why — prevents well-intentioned future developers from adding a broken filter. | Low |
| MT-L2 | Low | OPEN | `DeviceGroupsController.cs:167-168` | Returns `BadRequest("One or more devices do not exist in this tenant.")` on `SetMembers` validation failure, confirming to the caller exactly which validation aspect failed. An attacker can enumerate cross-tenant device IDs by observing whether the error message references tenant boundary. | Error message leaks tenant isolation model details. Change to `"One or more device IDs are invalid."` — same information to legitimate callers, no boundary information to attackers. | Low |
| SEO-L1 | Low | OPEN | `lib/seo.ts` | No page emits a `WebSite` schema block. Google uses `WebSite` to associate the site name with the URL in SERPs and for the Sitelinks Searchbox feature. Even without a custom search feature, a minimal block reinforces the brand entity connection in the Knowledge Graph. | Missing `WebSite` schema weakens Google's site-entity association. Add a `websiteLd()` helper emitting a minimal `WebSite` block with `name`, `url`, and `alternateName`. Inject on the home page. | Low |
| SEO-L2 | Low | OPEN | `public/sitemap.xml`; `prerender-seo.mjs:575-598` | `public/sitemap.xml` has hardcoded `lastmod` dates from May 2026 and does not reference `/legal/privacy/`, `/legal/terms/`, or `/privacy-policy/`. The prerender script regenerates `sitemap.xml` into `dist/` with all routes. If the prerender step is skipped (e.g., manual deploy without build), search engines receive the incomplete `public/` version. | Incomplete static sitemap served when the build step is skipped. Legal pages — which contribute E-E-A-T trust signals — are absent from the static version. | Low |
| SEO-L3 | Low | OPEN | `nginx.conf:16` | Docker Compose `nginx.conf` sets `script-src` to `'self'` with no `'unsafe-inline'` and no hash/nonce. Prerender script injects JSON-LD as inline `<script type="application/ld+json">`. Browsers don't execute `application/ld+json` blocks as scripts, but strict CSP parsers may flag them. Only affects self-hosted Docker deployments. | Self-hosted Docker deployments may see CSP violations in browser console for prerendered JSON-LD. Add explicit CSP exception for `script type="application/ld+json"` in `nginx.conf`, or document the behavior in the self-host README. | Low |
| SEO-L4 | Low | OPEN | `public/robots.txt` | `Content-Signal: search=yes, ai-input=yes, ai-train=no` is not a recognized field in the Robots Exclusion Protocol (RFC 9309) and is silently ignored by all major crawlers. Individual `Allow:` lines are redundant since `Allow: /` already permits all paths. | Non-standard directive does nothing. Use TDMRep at `/.well-known/tdmrep.json` for AI training opt-out (the emerging standard). Remove redundant individual `Allow:` lines. | Low |
| SEO-L5 | Low | OPEN | `public/robots.txt`; `public/sitemap.xml` | Static `public/sitemap.xml` does not include `/legal/privacy/` or `/legal/terms/`. Google uses Privacy Policy and Terms of Service links to assess site trustworthiness (E-E-A-T signals). The prerender-generated `dist/sitemap.xml` includes them, but the static file is the fallback. | Legal pages absent from static sitemap = missed E-E-A-T trust signals when the build step is skipped. Two-line addition to `public/sitemap.xml`. | Low |
| SEO-L6 | Low | OPEN | `lib/seo.ts:90-94`; `index.html:35-39` | Twitter Card implementation omits `twitter:site` and `twitter:creator`. Without them, Twitter/X cannot associate the card with a verified account, reducing card display prominence and preventing a 'Follow' button from appearing alongside shared links. | Missing Twitter card author association reduces social share prominence. If a Twitter/X handle exists, one-line addition to `useSeo` and `index.html`. | Low |
| SEO-L7 | Low | OPEN | `lib/seo.ts:71`; `prerender-seo.mjs:547` | Both the runtime hook and prerender script set `og:type` to `'website'` for every page including documentation articles. The JSON-LD correctly uses `TechArticle` for docs pages, making the OG type the only inconsistency. `og:type='article'` improves previews on Facebook and LinkedIn for documentation pages. | OG type mismatch between JSON-LD (`TechArticle`) and Open Graph (`website`) on docs pages. Extend `SeoOptions` with an optional `ogType` parameter and pass `ogType='article'` from each docs page. | Low |
| SEO-L8 | Low | OPEN | `public/` | No OpenAPI/Swagger JSON or YAML file at any standard path. `.well-known/api-catalog` links to `/docs/api/` (HTML). AI coding assistants and API client generators cannot auto-discover the API schema. Partners building integrations have no machine-readable contract. | No machine-readable API spec. Generate or hand-author an OpenAPI 3.1 specification; publish at `/openapi.json`. Update `.well-known/api-catalog` to reference it. | Low |
| SEO-L9 | Low | OPEN | `lib/seo.ts:161`; `Pricing.tsx:163-175` | `seo.ts` exports `pricingProductLd()` emitting a `Product` schema with `AggregateOffer`. The Pricing page `useSeo` call passes only `BreadcrumbList`. After React hydrates, the runtime JSON-LD replaces the static prerendered one with only `BreadcrumbList` — the `Product` schema enabling Google's structured pricing results is lost. | Product schema with AggregateOffer is built but never wired up on the Pricing page. Add `pricingProductLd()` to the `jsonLd` array in the Pricing page `useSeo` call. | Low |
| REST-L1 | Low | OPEN | `AuthController.cs:404` | `POST /api/auth/register/init` and `POST /api/auth/register` both return `Ok(...)` (HTTP 200) on successful resource creation. RFC 9110 specifies 201 Created for resource creation. The `register/init` endpoint could additionally return 202 Accepted since approval is asynchronous. | Wrong HTTP status codes for resource creation. Minor but breaks REST semantics and may confuse clients that check for 201 vs 200. Change to `CreatedAtAction(...)` or `StatusCode(201, ...)`. | Low |
| REST-L2 | Low | OPEN | `NotificationsController.cs:227`; `notifications.ts:98` | The `Send` action correctly uses `Accepted(...)` (HTTP 202) but the TypeScript client's error handling and callers are typed assuming synchronous creation semantics, not async accepted-for-processing semantics. UI copy says "sent" rather than "queued for delivery." | Client-server status code contract mismatch. Frontend should reflect asynchronous delivery nature in UX copy — "Notification queued for delivery" rather than "Notification sent." | Low |
| REST-L3 | Low | OPEN | `BlocklistController.cs:66` | `return CreatedAtAction(nameof(List), new BlocklistEntryResponse(...))` — the DTO is passed as the `routeValues` argument instead of as the `value` argument. The `Location` header is malformed (serialized DTO in the path instead of route values). | Malformed `Location` header on 201 Created response for blocklist entries. Any client following the `Location` header to fetch the created resource will hit a 404. | Low |
| REST-L4 | Low | OPEN | `Program.cs` (middleware pipeline), all GET controllers | No `ETag`, `Last-Modified`, or `Cache-Control` headers on any GET response. The agent fetches `/api/devices/appearance-config` on every startup; without ETags it re-downloads unchanged configuration on every agent restart. Anonymous endpoints (`/api/agent/version`, `/api/agent/uninstall-script-info`) have no caching headers either. | Unnecessary re-downloads of unchanged data on every agent startup. Add `Cache-Control: no-store` on authenticated endpoints, `Cache-Control: max-age=300` and ETag on anonymous agent endpoints. Honor `If-None-Match` on `appearance-config` for 304 responses. | Low |
| REST-L5 | Low | OPEN | `SystemController.cs:84, 347-396, 909-950` | `TrialRequests` takes only first 100 rows. `Tenants` returns all in a single query. `Devices` returns up to 500 rows. None accept `page`/`pageSize` parameters or return total counts. | Unbounded or arbitrarily-capped platform-admin lists. As the platform grows, the Tenants list becomes a full-table scan returning a multi-MB payload. Add standard pagination with total counts. | Low |
| REST-L6 | Low | OPEN | `Dashboard/src/api/devices.ts:16-86` | `DeviceApiResponse` declares both `machineName?: string` and `deviceName?: string`. Backend always returns `deviceName`. Frontend `Device` interface exposes only `machineName`. Second alias: `lastPing` vs `lastSeen`. The normalizer adds 70 lines of translation code. | API/client model inconsistency requiring a 70-line normalizer. Pick one canonical name (`deviceName`; backend wins) and remove the aliases from `DeviceApiResponse`. | Low |
| REST-L7 | Low | OPEN | `Program.cs` (`AddControllers` configuration) | No `[Consumes("application/json")]` on any controller and no middleware-level Content-Type enforcement. A request with no `Content-Type` may fail silently with model binding errors rather than a clear 415 response. | Missing Content-Type enforcement means clients get opaque model-binding failures rather than a clear 415 Unsupported Media Type. Add `[Consumes("application/json")]` globally or per-controller. | Low |
| REST-L8 | Low | OPEN | `DeviceGroupsController.cs:144` | `return Ok()` on successful `AddMember` returns HTTP 200 with empty body. RFC 9110 recommends 204 No Content for successful void mutations, which is used consistently in the same codebase elsewhere (Decommission, UpdateSettings, UpdateRole, etc.). | Inconsistent HTTP status for void mutations. Change `return Ok()` to `return NoContent()` — one character. | Low |
| REST-L9 | Low | OPEN | `BillingController.cs:210-213` | When Stripe secret key is missing or is the placeholder, `GET /api/billing/invoices` returns `Ok(new { invoices = Array.Empty<object>() })`. Admin UI displays "No invoices" with no indication of a configuration problem. Inconsistent with `/api/billing/checkout` and `/api/billing/portal` which correctly return 503 in the same condition. | Silent misconfiguration: Stripe-unconfigured billing shows "No invoices" rather than a clear "Stripe not configured" error. Admins will debug why there are no invoices without knowing the key is missing. Return 503 consistently with the other Stripe endpoints. | Low |
| PERF-L1 | Low | OPEN | `AppDbContext.cs` — `Notification` entity config (lines 91-102, 138-142) | No standalone index on `Notification.TenantId`. History, moderation-pending, and notification GET queries all add `WHERE TenantId = @p` via the global filter but have no supporting index. `NotificationDelivery` also has no index on `TenantId`. | Full-table scans on every tenant-scoped notification query. Add `HasIndex(n => n.TenantId)` for `Notification`, composite `(TenantId, CreatedAt DESC)` for History pagination, and `HasIndex(d => d.TenantId)` for `NotificationDelivery`. | High |
| PERF-L2 | Low | OPEN | `AppDbContext.cs` — `Device` entity config (lines 46-54) | Every tenant-scoped query against `Devices` executes `WHERE TenantId = @p AND Status != 3` with no index on `TenantId`. The only explicit index is on `RegistrationToken`. For a platform with many tenants, every device list query is a full-table scan filtered post-read. | Full-table device scans per request. Add `HasIndex(d => new { d.TenantId, d.Status })`. A partial index excluding `Decommissioned` devices would be smaller and faster for the common active-device filter. | High |
| PERF-L3 | Low | OPEN | `DeviceGroupsController.cs:228-234` | `GroupNameExists` — `g.Name.ToLower() == normalized` translates to `LOWER(Name) = LOWER(@name)` in SQL. Without a functional index on `LOWER(Name)`, high-volume group name lookups do sequential scans. | Case-insensitive name lookup prevents B-tree index use. Add a functional index on `LOWER(Name)` or use `EF.Functions.ILike` with a case-insensitive collation to restore index usage. | Low |
| PERF-L4 | Low | OPEN | `DevicesController.cs:170-179` — `List()` | Loads every active device with `GroupMemberships` + `Group` details via eager loading in one cartesian JOIN. No `Skip`/`Take`. A tenant with 2,000 active devices produces 2,000+ rows in a JOIN with group data. Both the React Devices page and the Dashboard call this on every mount. | Unbounded JOIN payload on every page load. Add optional `page`/`pageSize` parameters with a hard cap. For Dashboard, consider a lightweight `/api/devices/summary` endpoint returning aggregate counts only. | High |
| PERF-L5 | Low | OPEN | `AnalyticsController.cs:70-80` — `Volume()` | `GroupBy(n => n.SentAt!.Value.Date)` applies a date-truncation function. Without a functional index on the truncated date, the DB must read all rows in the time range and apply the function per row. At volume, this dominates the analytics query execution plan. | Date-truncation grouping without a functional index = sequential scan + per-row function evaluation. Add a persisted computed column `SentDate DATE GENERATED ALWAYS AS (sent_at::date) STORED` with an index, or add a functional index on `DATE(sent_at)`. | Low |
| PERF-L6 | Low | OPEN | `Dashboard/src/pages/History.tsx:114-118` | `filtered` applies title/status search against the local `notifications` array (just the current page, 25 items). Searching for a title on page 3 returns zero results. The user believes search is broken. | Client-side search on server-paginated data is broken by design — it only searches the visible page. Pass `?search=` to `GET /api/notifications` and apply server-side. Reset page to 1 when search term changes. | Medium |
| PERF-L7 | Low | OPEN | `Dashboard/src/pages/Compose.tsx:140-150` | Every Compose mount fires three parallel API calls: devices list (unbounded), groups list, templates list. Navigate away and return, all three fire again with no caching. At 1,000+ devices the devices payload dominates. | Three uncached API calls per Compose mount. Cache device and group lists in a shared React context or SWR/React Query with a short stale-while-revalidate window (e.g., 30 seconds). | Low |
| PERF-L8 | Low | OPEN | `Dashboard/src/pages/Dashboard.tsx:43-56` | `sent7d` count and `deliveryRate` run as plain expressions on every render. Pattern is inconsistent with `useMemo` usage elsewhere (e.g., `Devices.tsx`). | Redundant recomputation on every render. Wrap both in `useMemo(() => ..., [notifications])` and `useMemo(() => ..., [devices])`. | Low |
| PERF-L9 | Low | OPEN | `Dashboard/src/pages/Devices.tsx:729-735` — `DeviceGroupModal` | `visibleDevices` is a plain `devices.filter(...)` with no memoization. Every keystroke causes a full re-render re-filtering the entire device list. With 1,000+ devices, noticeable jank at ~16ms per keystroke. | Unmemoized filter on a large list. `useMemo(() => devices.filter(...), [devices, search])` is a one-liner that eliminates the per-keystroke full scan. | Low |
| PERF-L10 | Low | OPEN | `Dashboard/src/pages/Analytics.tsx:2-5` | Recharts is approximately 300 KB gzipped, imported at component level. Analytics is not a first-load route, so this weight is paid on every initial page load unless the route is lazy-loaded. | 300 KB added to initial bundle for a non-first-load route. Wrap the Analytics page in `React.lazy()` in the router — Vite handles code-split automatically. One-liner change. | Low |
| REL-L1 | Low | OPEN | `UpdateService.cs:199-210` | Creates a fresh `UpdateManager(GetFeedUrl())` on every `ApplyUpdateAndRestart` call. `_pendingUpdate` holds state from a different `UpdateManager` instance than what apply uses. If the Velopack update server is unavailable during apply, the manager may block indefinitely, freezing the tray UI. | Stale update state and potential UI freeze on apply. Cache the `UpdateManager` from `CheckAndDownloadAsync` and reuse in `ApplyUpdateAndRestart`. Run on a background thread with a timeout and UI loading state. | Low |
| REL-L2 | Low | OPEN | `NotificationQueueService.cs:215-233` | When a SignalR hub push fails, the delivery is immediately marked `Failed` with no retry. A transient SignalR connection blip (device mid-reconnect) permanently marks the delivery as Failed. Deliveries that transition to Failed before the agent reconnects are excluded from catch-up polling. | Transient SignalR errors permanently mark deliveries as Failed. 1-2 retries with short delay (or leave as `Pending` on hub push failure so catch-up serves them on reconnect) would avoid permanent failure on momentary disconnects. | Low |
| REL-L3 | Low | OPEN | `AuthController.cs:506, 669` | `await _sms.SendAsync(...)` is called inline in the login request path. If ClickSend is down or slow, the login request hangs up to 100s. No `CancellationToken` threaded from `HttpContext.RequestAborted` to SMS/email calls. | Login request held open for up to 100s during provider outage. Pass `HttpContext.RequestAborted` to `SendAsync` calls. Set a short per-call timeout on both service `HttpClient` instances. For login SMS, the code is persisted to DB before send — consider queuing fire-and-forget and returning the challenge immediately. | Low |
| DEVOPS-L1 | Low | OPEN | `src/ToastRevival.Api/Properties/launchSettings.json:17`; `Program.cs:436-440` | All three launch profiles set `launchUrl` to `'swagger'` and `ASPNETCORE_ENVIRONMENT` to `'Development'`. `Program.cs` correctly gates Swagger behind `IsDevelopment()`. Standard and acceptable for development. | Add an integration test verifying `GET /swagger/index.html` returns 404 when `ASPNETCORE_ENVIRONMENT` is `Production`, preventing the gate from being accidentally removed in a future refactor. | Low |
| DEVOPS-L2 | Low | OPEN | `scripts/build-release.ps1`; agent update configuration | Velopack update feed URL is hard-coded at build time. Self-hosted deployments have no documented mechanism to point the agent at their own update feed. | Self-hosted customers cannot redirect agents to their own update server without a custom build. Expose the update feed URL as a build-time parameter and as a runtime registry key or config file value. | Low |
| DEVOPS-L3 | Low | OPEN | `scripts/sign-msix.ps1` | MSIX signing requires a physically present Thales SafeNet USB hardware token with an interactive PIN dialog and `signtool.exe` on the build machine. MSIX signing is impossible in any headless CI runner or cloud build environment. | Hardware-token-only signing blocks CI/CD automation for MSIX builds. Document certificate expiry date and renewal lead time. Evaluate EV certificate in Azure Key Vault (signable via `AzureSignTool` without a physical token). | Low |
| DEVOPS-L4 | Low | OPEN | `src/ToastRevival.Api/appsettings.Development.json` | Currently only overrides logging levels (benign), but its presence in Release publish output means it is copied into the Docker image. If a future development override contains sensitive configuration, it would ship in the production image with no explicit exclusion. | Development config present in production image. Add an explicit exclusion to the Dockerfile or `dotnet publish` command to strip `appsettings.Development.json` from publish output. Establish the pattern before more sensitive development overrides are added. | Low |
| WSEC-L1 | Low | OPEN | `ToastTemplates.cs:457-458` (`ApplyAudio`) | Any `ms-winsoundevent:` URI string passes the scheme check. While this scheme is not known to enable arbitrary file or network access, the accepted form is broader than necessary. The WinRT audio subsystem treats unrecognized event names as silent. | Broader-than-necessary URI acceptance. Validate `ms-winsoundevent:` URIs against a whitelist of known event name prefixes (`Notification.`, `Alarm.`, `Reminder.`, `Call.`) to prevent future attack surface if the scheme's behavior changes. | Low |
| WSEC-L2 | Low | OPEN | `scripts/sign-msix.ps1:67-72` | `signtool /a` auto-selects the best-matching certificate. On a developer machine with multiple code-signing certificates installed, `/a` could select an unintended certificate. The post-sign verification step catches this at build time but not at agent update verification time. | Auto-certificate-selection risk. Add `/sha1 <thumbprint>` to pin to the specific Sectigo OV certificate. Verify the output thumbprint in the post-sign block against the expected value (19B07B46712C2D87FF6AA99842F7EF6B036FEDA7). | Low |
| WSEC-L3 | Low | OPEN | `DesktopOverlayService.cs:118-119` | No length cap or character class restriction on rendered `CustomText`. An extremely long string or wide Unicode characters could cause `boxW`/`boxH` dimensions to grow to device-resolution-scale multiples, potentially exhausting GDI object memory. Threat requires a compromised signing key or server. | GDI resource exhaustion via oversized custom text. Clamp `CustomText` to a reasonable maximum (256 characters) and add a defensive maximum on `boxW`/`boxH` to prevent abnormally large GDI bitmap allocations. | Low |
| WSEC-L4 | Low | OPEN | `Program.cs:705` (`--diag` mode) | Writes diagnostic log to `%TEMP%\toastnotification-diag.txt` with no explicit security descriptor. Predictable filename allows any process running as the same user to read or overwrite it while being written. Log content includes device IDs, server URLs, connection states, and timing information. | Predictable-path diagnostic file with world-readable default ACL for same-user processes. Use a randomly named temp file (`Path.GetTempFileName`) or write to a user-scoped path with an explicit restrictive ACL. | Low |

## Informational

| ID | Severity | Status | File:Line | What's wrong | Why it matters | Confidence |
|---|---|---|---|---|---|---|
| INJ-I1 | Info | OPEN | `client.ts` (all POST/PUT/PATCH/DELETE calls) | Bearer-only authentication means same-site CSRF is not a current risk — CSRF attacks require the browser to attach credentials automatically (cookie-based auth), which this SPA does not use. However, any future cookie-based auth path would have no CSRF protection in place. | Document the Bearer-only assumption explicitly in a code comment or ADR. Ensure any future cookie-based auth path includes `SameSite=Strict` or an anti-CSRF token as a precondition of implementation. | High |
| DC-I1 | Info | OPEN | `scripts/generate-app-icon.ps1` | Generates `ToastNotification.ico` from a source PNG. The `.ico` file is likely already committed. One-time tool with no CI reference and no documentation of when it was last run or what input PNG was used. | Orphaned one-time utility with no context. Add a comment header noting when it was last run and what input PNG was used, or delete if the icon is permanently committed and the source PNG is available. | Low |

---

## ANCHOR-CHALLENGE

None this pass.

---

## Reviewed and clean (off gauge — prose, not counted)

Surfaces personally verified this pass (in addition to the false-positive triage above):

- **EF global query filter coverage** — `AppDbContext.cs` confirmed: `AppUser` (line 43), `Device` (53), `DeviceGroup` (62), `DeviceGroupMember` (79), `Notification` (88/101), `NotificationDelivery` (117), `AuditLog` (126), `TenantBlocklistEntry` (152), `TenantApiKey` (165), `EnrollmentToken` (182) all have `HasQueryFilter` keyed to `_tenantProvider.TenantId`. VERIFIED-CLEAN.
- **ModerationController** — `GetPending()` relies on the Notifications global filter (confirmed); `Approve()` and `Reject()` each independently fetch with an explicit `n.TenantId == tenantId` predicate before mutation. Audit-logged. VERIFIED-CLEAN.
- **BlocklistService.NormalizeForMatch** — `BLK-1` anchor in code documents the NFKC + format-char-strip approach and its known limitation (cross-script homoglyphs deferred to Azure Content Safety). Sound and intentional. VERIFIED-CLEAN.
- **NotificationQueueService background path** — orphan recovery (`RecoverOrphansAsync`) and scheduled backfill (`EnqueueDueScheduledAsync`) correctly use `IgnoreQueryFilters()` with a fresh DI scope. `ProcessAsync` also uses `IgnoreQueryFilters()`. No tenant-filter bypass risk. VERIFIED-CLEAN.
- **SelfUpdateService.cs post-Agent-M1 fix** — `EnsureProtectedVerifiedDir` deletes any pre-existing entry (reparse-point: unlink only; plain dir: `Delete(recursive:true)`); `LockDownToSystemAndAdmins` calls `sec.SetOwner(system)` before setting ACEs. The Agent-L1 anchor's "SYSTEM-owned" premise is now enforced in code. VERIFIED-CLEAN.
- **WiX installer post-Wix-M1 fix** — `UninstallApplyTask` custom action present and sequenced `After="UninstallUpdaterTask"` on `REMOVE="ALL"`. All three `\Toast2IT\*` tasks have uninstall counterparts. VERIFIED-CLEAN.
- **Dashboard XSS surface** — no `dangerouslySetInnerHTML`, `innerHTML`, or `eval` anywhere. `AuthContext.tsx` localStorage anchor (`REVIEW-2026-05-25 INFO-01 REJECTED-by-design`) confirmed as intentional pending a dedicated migration milestone. CSS custom properties verified against `index.css` — no undefined vars. VERIFIED-CLEAN.
- **Version consistency (v0.4.38)** — `appsettings.json` `Agent:LatestVersion` `0.4.38` == `ToastRevival.Agent.csproj` `0.4.38` == `Package.appxmanifest` `0.4.38`. VERIFIED-CLEAN.
- **install-toast-agent.ps1 (main script)** — Authenticode enforcement exits code 3 on bad signature (lines 272-287); `$null = $proc.Handle` materialized before `WaitForExit` (RMM-H2 fix confirmed present). VERIFIED-CLEAN.
- **RMM scripts (all four)** — `[gc]::WaitForPendingFinalizers()` + checked `reg unload` retry present in both sites (RMM-M1 fix); `RMM-M2 REJECTED-by-design` anchor present in `Reset-ToastLockScreen.ps1` headless branch. VERIFIED-CLEAN (except template Authenticode advisory-only, filed as RMM-M3).

---

## Top fixes (do these first)

*From 2026-06-03 pass — both closed:*
1. **RMM-M3** *(FIXED-VERIFIED)* — Authenticode hardened in template.
2. **Agent-L2** *(FIXED-VERIFIED)* — `using var` at AgentClient.cs lines 42 and 735.

*From 2026-06-05 comprehensive review — by priority:*
3. **DEVOPS-C1** — Rotate all three GitHub PATs and the server password in `Docs/ToastRevival/.env` and `.env.pats` immediately. Private PAT has `repo` scope. Add git-secrets or truffleHog as pre-commit hook.
4. **DEVOPS-H1** — Fix `Jwt__MfaElevationExpiresInMinutes: 480` in `docker-compose.yml` to 15. This single line defeats the MFA step-up model that `RequireFreshMfa()` guards enforce across the platform.
5. **MT-H1/MT-H2/MT-H3/MT-H4/MT-H5** — Add explicit `TenantId` predicates to `AnalyticsController` (all three endpoints), `NotificationsController.History`, `DevicesController.List`/`Get`, `DeviceGroupsController.AddMember`/`SetMembers`, and `NotificationsController.ResolveTargetDeviceIds` (`TargetType.All`/`TargetType.Device`). Single implicit filter layer on all these paths.
6. **WSEC-H1** — Set `fdwRevocationChecks = WTD_REVOKE_WHOLECHAIN` in both `UpdateService` and `SelfUpdateService`. Revocation checking disabled entirely enables compromised-certificate update delivery with SYSTEM execution.
7. **REL-H1** — Add `UseExceptionHandler`/`AddProblemDetails` and adopt RFC 9457 ProblemDetails uniformly. No central exception handling currently.
8. **REL-H3** — Add Polly retry + circuit-breaker to Mailjet, ClickSend, Stripe, Turnstile, Azure Content Safety, and Entra `HttpClient` registrations.
9. **DOS-H1/DOS-H2** — Add `[EnableRateLimiting]` to `POST /api/auth/forgot-password` and `POST /api/auth/register/verify-sms`. Add `RegisterFailedSmsAttemptAsync` to the registration OTP path.
10. **AA-M6** — Replace SSO callback JWT-in-fragment with a short-lived one-time code exchange.
11. **DEVOPS-H2/DEVOPS-H3** — Add `.dockerignore` and configure `dotnet publish` to emit no PDBs in the Release image.
12. **DEVOPS-H4** — Create a minimal `ci.yml` GitHub Actions workflow running `dotnet build` and `dotnet test` on every push/PR to main.
13. **XT-M1** — Machine-SID token binding (build-mode project XT-3, owner: Keith). Stays OPEN until XT-3 ships.
14. **PERF-L1/PERF-L2** — Add missing DB indexes on `Notification.TenantId` and `Device.TenantId`. These are full-table scans on the highest-frequency query paths.

---

*Lifecycle: COLD REVIEW pass. All new rows start OPEN; fix team drives terminal states (FIXED-VERIFIED / REMEDIATED / REJECTED-VERIFIED). Gauge reads the pipe-table rows — do not alter the format.*
